# 夏雲シミュレーター 計画書

## 0. 背景・経緯

「幻想桜暦」（一本桜×四季ダイヤルの大型企画）は難航したため一旦中止し、資産は `bk/` に退避した（`bk/proposal.md`, `bk/season-transition-animation.md`, `bk/app/`, `bk/art-source/` 参照）。

`bk/agent-workflow-policy.md` §1.5 に記録されている前回の失敗の本質は、**空・山などのビジュアル要素を「文章で雰囲気を説明→実装者が感覚で数値を決め打ち」で作った結果、参考画像と全く違う見た目になった**こと。今回はこれを踏まえ、さらに一歩進めて「見てから直す」修正ループそのものを無くす方針を取る。

## 1. コンセプト

固定カメラで空だけを描く背景レイヤー。イメージは、ベンチに座る女の子を正面から見た構図の背景に広がる夏の空と雲。前面（ベンチ・女の子）は別途・別レイヤーで作るため、このプロジェクトは**空と雲のみ**を扱う。地平線は画面下1/3付近に置き、下側は前面レイヤーを重ねるための余白として空けておく。

時間帯は日中→夕焼け→日没（薄暮）の一続きの弧を、自動タイムラプス再生＋手動スライダーの両対応で表現する。

ビジュアルの方向性：リアル寄りの新海誠風（積乱雲の存在感、劇的な夕焼けのグラデーション、透明感のある光）。

## 2. 大方針：数学的決定論

**色や形を「参考画像を見ながら手で調整する」のではなく、実在する物理法則・数式から導出する。** パラメータ（時刻・座標・乱数シード）が決まれば出力が一意に決まるようにし、「レンダリング→目視→修正」のループを構造的になくす。

これは前回の失敗（§1.5）とも整合する。前回禁止されたのは「実装者の"良い感じ"の感覚で色や形を選ぶこと」であり、今回は感覚の代わりに物理方程式・気象学の理論を根拠にする。参考画像への目視合わせという工程自体を、大気散乱・放射伝達・熱上昇流理論という「既に答えが出ている数式」に置き換える。

| 見た目の要素 | 決定論的な根拠 |
|---|---|
| 空の色（昼〜夕焼け〜薄暮） | 大気散乱シミュレーション（レイリー散乱＋ミー散乱、実際の散乱係数・スケールハイトを使用） |
| 太陽の位置・色 | 太陽高度角を時刻の関数として定義し、大気散乱と同じ物理式で色温度が自然に導出される |
| 雲の陰影・立体感 | 放射伝達方程式の近似（Henyey-Greenstein位相関数、Beer-Lambert減衰、エネルギー保存マルチスキャッタリング近似、パウダー効果） |
| 雲の形・密度 | 決定論的ノイズ関数（Worley/FBM、乱数シード固定） |
| 雲の流れ | カールノイズ（発散ゼロのベクトル場、高度依存の風速シア） |
| 積乱雲の湧き上がり | 熱上昇流（サーマル）理論に基づく成長曲線（簡略化した閉形式、タワーごとに固有の周期でライフサイクル） |
| 色調（グレーディング） | 露出・太陽高度から連続的に導かれる固定の式（キーフレームごとの手動hex指定はしない） |

## 3. 技術方針の詳細

### 3.1 空：大気散乱シミュレーション
- レイリー散乱（波長依存の分子散乱、青空の起源）とミー散乱（エアロゾルによる散乱、太陽周辺の白っぽいハロー）を、実測に基づく散乱係数・スケールハイト（レイリー層厚8km、ミー層厚1.2km）を用いて数値積分（プライマリレイ＋サンレイの二重積分、標準的なリアルタイム大気散乱の手法）。
- シーン座標はこの層では「1ユニット=1km」とし、仮想惑星半径6371kmの球面上にカメラを置く形で計算する（惑星曲率まで含めて物理的に正しい光路長を扱う）。
- 太陽が沈むにつれ光路長が伸びて青が散乱し尽くされ赤が残る、という夕焼けの色変化が式から自然に出る。手動でのグラデーション色指定は行わない。

### 3.2 太陽位置
- 時刻パラメータ `t ∈ [0,1]`（日中→夕焼け→日没）から太陽高度角を決定論的に導出する滑らかな曲線（3キーフレーム：日中高高度／夕焼け水平／薄暮地平線下、を滑らかに補間）。方位角は構図固定のため一定値とし、高度角のみ変化させる。
- 太陽の色（色温度）は上記の大気散乱と同じ物理式から、太陽高度に応じた光路長分の減衰として導出する（個別に暖色hexを置かない）。

### 3.3 雲：ボリューメトリック・レイマーチング
- カメラ固定のため全天球ではなく画面に映る範囲のみをレイマーチし、負荷を抑える。
- 密度場は「背景の層積雲・小さい積雲」（低周波FBMによる被覆マスク、乱数シードで毎回配置が変化）と「主役の積乱雲タワー」（画面内の構図上の要となる位置は固定し、湧き上がり方は乱数化）の2層で構成。
- 積乱雲タワーの鉛直方向の成長は、サーマル理論に基づく簡略化された閉形式の成長曲線 `growth(τ)` で駆動する（τ はタワー固有のライフサイクル内経過時間、乱数シード+グローバル時刻から一意に決定）。生成→成長→（消散）のサイクルをタワーごとに非同期に回すことで、同じ画面を見続けても飽きない変化を作る。
- 雲の流れはカールノイズによる水平方向のドメインワープ＋高度依存の風速シアで表現。
- ディテール（カリフラワー状の凹凸）はWorley-FBMハイブリッドノイズをカールノイズで移流させたもの。
- ライティングは Dual-lobe Henyey-Greenstein位相関数（前方＋後方散乱）、太陽方向への数ステップの光線マーチによるセルフシャドウ、Beer-Lambert減衰、エネルギー保存マルチスキャッタリング近似（Schneider方式のオクターブ減衰近似）、エッジのパウダー効果を組み合わせる。太陽グローも同じ位相関数から自然に生じるため、従来のような `pow(sunDot, N)` の手作業ハックは不要。

### 3.4 固定カメラを活かした最適化
- カメラが動かないため、毎フレーム全画素を計算せず、フレーム間でサンプルを間引いて時間的に蓄積・再投影する（temporal accumulation）ことで、リッチなライティング計算の予算を確保する。パフォーマンスチューニングは物理モデルを崩さない範囲（サンプル配置・蓄積方式）で行い、色や形の恣意的な調整はしない。

### 3.5 グレーディング
- 彩度・コントラスト・ティール&オレンジのスプリットトーンも、太陽高度や露出から連続的に導かれる固定のトーンカーブ/パラメトリックLUTとして実装し、キーフレームごとの手動色指定はしない。

## 4. 実装フェーズ

1. **土台**：`app/` を vite + three.js + TypeScript で新規構築（レンダラー/カメラ設定の考え方は `bk/app/src/core/` を踏襲しつつ数値は新構図用に引き直す）。
2. **太陽位置モジュール**：時刻→太陽高度角の決定論的な関数。
3. **大気散乱シェーダー**：フルスクリーンのレイ再構成＋数値積分によるレイリー/ミー散乱。
4. **ボリューメトリック雲シェーダー**：背景の層積雲＋主役の積乱雲タワー、カールノイズによる流れ、サーマル成長則による湧き上がり、放射伝達近似によるライティング。
5. **時間帯コントローラー**：自動タイムラプス＋手動スライダー。
6. **PostFX**：ブルーム、決定論的トーンカーブによるグレーディング。
7. **固定カメラの構図確定と合成用ドキュメント化**：地平線の画面内位置・カメラFOVを記録し、後で前面（ベンチ・女の子）レイヤーと違和感なく重ねられるようにする。
8. **パフォーマンス調整**：temporal accumulationの導入、モバイル実機確認。

## 5. スコープ外（今回やらないこと）

- 前面（ベンチ・女の子）のモデリング・アニメーションは別途・別プロジェクトで行う。このプロジェクトは空・雲のみ。
- 参考画像を見ながらの色・形の手動チューニングは行わない（§2の方針）。
