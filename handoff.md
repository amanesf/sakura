# 引き継ぎ: 縦画面レイアウトと空の多層化が入った、次は雲の「ポップコーン」を潰す

「参考画像に数値フィットさせる」フェーズは終わり、**アプリは参考画像
そのもの＋窓の外だけが動く空**になった。その上に今回、**縦画面レイアウト**と
**空の多層化・雲の一生**を入れてある（§2.5）。**次にやることは1つ: 雲の見た目。
球が並んで見える（ポップコーン／ぶどうの房）状態を潰す。**

## 0. 前提

- 作業ブランチ `claude/mobile-portrait-layout-75pkk3`。ここで開発し、
  ここに push。PR は明示的に頼まれるまで作らない。
- 計画は **`image-sky-plan.md`**（`plan.md` は §3.3/3.4 がこれに差し替わっている）。
- アプリは `app/`（vite + three.js + TypeScript）。対象端末は
  **Pixel 10 Pro の縦画面**（約 448x998）。横画面ではない（§2.5 で変更）。
- 参考画像は2枚。**`1786443741198.png`**（原画）と
  **`1786511966180.png`**（ユーザーが空を一色のマゼンタで塗った版）。
  後者はマット専用で、絵として使うのは常に前者。
- **大原則（plan.md §2）**: 色や形を「レンダリング→目視→勘で数値を直す」で
  決めない。参考とレンダの両方に同じ統計を当てて数字で判断する。コミット
  メッセージには「何をどう測って、どの数値がどう動いたか」を必ず書く。
- **「線画は足さない」** — ユーザーからの明確な禁止事項。
- **`scripts/capture.js` は既定で `?fit=frame` を付ける。** アプリの実レイアウト
  ではキャンバスがページ上部の帯なので、そのまま 1408x768 で撮ると 1619x353 の
  切り抜きになり、README のクロップ矩形が全部ずれる。`fit=frame` は絵に
  ビューポート全体を渡す（＝これまで通りの計測フレーム）。実レイアウトを見たい
  ときだけ `fit=page`。
- 計測ハーネスの使い方は `scripts/README.md`。**`scripts/capture.js` は
  playwright を require するが root の package.json には入っていない。**
  セッション開始時に一度
  `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm install --no-save playwright` が必要。
  root で `npm install`（sharp）も要る。1キャプチャ約3-4分。

## 1. 今のアプリの構造

```
      index.html / style.css  縦画面レイアウト（タイトル / 絵の帯 / コンソール）
後   scene/sky.ts          大気散乱（RMSE 3.1 でフィット済み、触らない）
中   scene/cloudField.ts   雲のフィールド（層・沸く・流れる・消える・天気）
      └ scene/clouds.ts    クラスタ1個の生成（パフの散らばり・形・遮蔽・沸き）
      └ scene/cloudShader.ts 雲の陰影（ランプ・ヘイズ・リム）
     effects/horizonHaze.ts 水平線付近のぼかし＋ヘイズ寄せ
前   effects/plateShader.ts 参考画像のプレートを最後に合成
```

- `scripts/plate.js` が2枚の参考画像から `app/public/plate.webp`
  （RGBA 1408x768、130KB、フレームの **40.1%** がα=0）を作る。
  マゼンタからの距離 105 未満を二値で空にし、囲まれた島（ガラスの映り込み）は
  フラッドフィルで吸収。RGB は常に原画から取るので紫は混入しない。
- `core/frame.ts` が「1408x768 のどのサブ矩形を映すか」を決め、**3Dカメラの
  `setViewOffset` とプレートの UV に同じ矩形**を渡す。引き伸ばさない。
  縦の切り落としは上30%/下70%（塔の冠が y≈77 にあるため）。
- 時間は `simTime`（秒）1本。**シーンは simTime の純関数**で、`?t=` はそれを
  直接指定する。倍速スライダー（1-30倍）をどこに置いてもキャプチャは再現する。

## 2. 前回やったこと（数値）

| 項目 | 前 | 後 |
|---|---|---|
| くり抜き面積 | 36.8%（自前の色分類） | **40.1%**（手キーの二値マット） |
| 紫の縁・映り込み | 残っていた | 消えた（許容距離 105、島は塗り潰し） |
| 下層の雲量 | 0.3+0.7w | **smoothstep(w, 0.02, 0.42)**（夏空以上で満杯） |
| 各層の個数 | 18 / 16 / 11 | **26 / 22 / 15** |
| 塔が見える時間帯 | 最初の40分ゼロ | 層化サンプリングで常に1本以上 |
| 海と雲の境目 | 硬い | `horizonHaze.ts` でぼかし＋ヘイズ |

雲は固定47クラスタを揺らすのをやめ、**風上で生まれ→7m/sで流れ→成長して消え→
抜けたら新しい種で作り直す**フィールドになった。画面上の速度は幾何から出る
（16.9km で 0.36 px/s、横断102分）。天気は `weatherAt(simTime)` の低周波3項
（周期 約3.2h / 7.5h / 1.6h、値域は実測で 0.19-0.94）。

## 2.5 今回やったこと（縦画面と空の多層化）

### 縦画面レイアウト

`index.html` / `style.css` / `core/renderer.ts` / `core/frame.ts`。ページは
上から **タイトル（文言は未定、枠だけ）→ 絵の帯 → `.console`（下部の
コントローラー置き場、今はスライダーだけ）**。

- 絵は画面幅の **115%**、左に **-10vw** ずらす。実測で左 10.0% / 右 4.9% はみ出し、
  横スクロールは出ない（448/412/393 幅で確認）。
- 帯の高さは「115vw ÷ 1.8333（プレートのアスペクト）」と `--stage-max-height`
  (46vh) の小さい方。縦画面では前者が勝つのでプレート全面が出る＝
  `core/frame.ts` は切り抜きゼロ。デスクトップなど横長では 46vh が勝ち、
  **引き伸ばさずに `frame.ts` が切り抜く**（従来の規則のまま）。
- キャンバスは `.stage` のサイズ。`watchResize` は `ResizeObserver` でホストの
  **CSS サイズ**を報告するだけになり、バッファのサイズ決めは `main.ts` に移した。
- **レンダ解像度は参考フレームに固定（1408x768）。** `setPixelRatio(1)` ＋
  `setSize(rect.width, rect.height, false)`（第3引数 false で CSS は触らない）。
  postFx の定数（bloom 半径・kuwahara カーネル・macroContrast・ヘイズの texel）は
  全部**バッファのピクセル**で書かれていて、しかも 1408x768 で合わせてある。
  要素サイズに合わせると同じ半径が画面の 2.7倍を覆い、実機だけ計測フレームより
  眠い絵になっていた。固定したので**どの端末でも計測フレームと同じ絵**が出る。
  実機は 448 CSS × DPR3 = 1344 device px なのでほぼ等倍。
  実測: 縦画面 `fit=page` と `fit=frame` の同 t キャプチャの差は
  **平均絶対差 0.42 / 255、p90=1、差 8 超は 0.72%**。残差は `.stage` の CSS 箱が
  整数丸め（515x281 → アスペクト 1.8327）で `visibleRect` が 1407.5px 幅・
  x=0.25 の矩形を返すための **1/4 ピクセルのずれ**。引き伸ばさない規則を守った
  結果で、絵としては同一。

### 雲（ユーザー指摘の3点）

| 指摘 | 入れたもの |
|---|---|
| 流れるだけでサイズが変わらない | `cloudField.cloudBulk(tau)`（一生をかけて膨らみ→薄れる）と、`clouds.ts` の**ロブ単位の沸き**（各パフが自分の位相・周期で伸縮＋上下） |
| 形状がもっとランダムでいい | `ClusterShape`（平面異方性・ウィンドシアの傾き・ロブの粗さ・衛星数）をクラスタごとに抽選。半径プロファイルの定数もクラスタごとにジッタ。層数/パフ数もジッタ。ノジュール原型 5→12種 |
| 高層雲も流れてほしい | 層を2つ追加: **altocumulus**(4.6-6.0km, 風2.2倍) と **cirrus**(9-11km, 風4.2倍) |

リアルさのために入れた仕掛け（全部 `simTime` の純関数のまま）:

- **高度で風速が違う**。`TierSpec.windScale`。巻雲が下の積雲を追い越すのが、
  空が「層になっている」と読める一番安い手掛かり。
- **天気は上から来る**。`TierSpec.weatherLead` は `weatherAt()` を**先の時刻で**
  引く（巻雲 3600s、高積雲 1500s）。晴れているうちに巻雲が増え、低い雲が
  塞ぐ頃には巻雲はとっくに一面、という順序になる。
- **高層雲は影を落とさない**。`castsShadow: false` →
  `NO_SHADOW_CAST_LAYER`(=1) に置いて影パスのカメラ（レイヤ0）から外す。
  78km の light-space マップで 40km の巻雲が空全体を一律に暗くするのを防ぐため
  （実際の巻雲も光学的に薄い）。
- **高層の雲底は天気で下がらない**。積雲の雲底は持ち上げ凝結高度なので湿ると
  下がるが、高積雲・巻雲の高度は上空の気温と圏界面で決まる。`baseAlt < 4` の
  層だけ従来の天気補正を掛ける（掛けないと湿った日に巻雲が 9km→5km に落ちた）。

**まだ計測していない。** 上の変更は形と動きの話で、色ランプ・大気散乱・
`frame.ts` の切り出し規則には触れていないが、`softFrac` などの数字は動いている
はず。§3 の測り直しはこの状態から始めること。

**性能は測らない方針**（ユーザー判断）。クラスタ 77→105（追加分は1クラスタ
数十インスタンス）、レンダ解像度は実機で 1030x562 相当→1408x768 固定。

## 2.6 実機だけ雲が真っ黒だった件（FloatType の罠）

ユーザーの実機スクリーンショット（amanesf.github.io/sakura/）で、中央の雲が
**白い縁を残したまま真っ黒**になっていた。ヘッドレスのキャプチャでは一切
再現しない。

原因は `cloudRamp.ts` が色ランプを **`FloatType`（RGBA32F）＋ `LinearFilter`**
の DataTexture で作っていたこと。WebGL2 のコアでは RGBA32F は filterable では
なく、線形サンプラを使うには `OES_texture_float_linear` が要る。この拡張が
無い端末ではテクスチャが incomplete 扱いになり、`texture2D()` が
**(0,0,0,1)＝黒**を返す。SwiftShader は拡張を実装しているので、
**このプロジェクトの全キャプチャは正しく見えていた**。計測ループでは原理的に
捕まえられない種類のバグ。

症状の形が診断そのものだった: 黒い塊が**白い冠と淡い遠景の雲を保っていた**。
`cloudShader.ts` でこのテクスチャを通らない経路はその2つだけ——ハイライトは
`uWhiteHDR` ユニフォームへ、空気遠近は `uHazeColor` ユニフォームへ mix する。

修正は `HalfFloatType`（RGBA16F）。WebGL2 のコアで拡張なしに filterable なので
再発しない。ランプの最大値 8.157 は half float の範囲に十分収まり、往復誤差は
最大 2.6e-3。

## 2.7 タイトル・デザイン・空のプリセット（3回目のセッション）

### ページのデザイン

コンセプトは**「暗い計器盤に開いた、光っている窓」**。下部の黒い余白が死んで
いたのを、意図した暗がりに変える。

- タイトル「終わりを知らない、あの夏の境界線」＋英語サブタイトル。明朝体。
- `.ambience`（`index.html` の最初の要素）がページの光。絵の足元
  (`--stage-foot`) を中心にした radial 2枚＋下から上への linear 1枚。
- **絵の上には何も乗せない。** グラデ・フィルタを frame 全体にかけるのは
  ユーザーからの提案だったが、`plateShader` が参考イラストを最後に合成する
  ので女の子や窓枠まで染まるし、色ランプ（実測）と散乱（RMSE 3.1）の
  フィットが崩れて `tonedist`/`skyprofile` が全部ずれる。ページ側に置けば
  見た目は同じ効果で、絵の数値は一切動かない。
- 唯一の例外は `.stage::after`、絵の下端を紙に溶かす 14% のフェード。
  これも `fit=frame` では消える。
- **最初の版は radial の中心を viewport の 26% に置いていて、絵の裏に隠れて
  いた**（＝ページ下半分が真っ黒のまま）。光は絵から**下へ**こぼれる必要が
  ある。

### コンソール

絵の直下に移動（画面下端ではない）。プリセットのピル2つ＋細い発光スライダ。
**既定は 10倍速**（1倍だと最初の1分が静止画と区別できない）。

### 空のプリセット（`scene/skyPresets.ts`）

`weatherAt` を「生の振動 `weatherOscillation` を、プリセットの窓に写像する」
形に分解した。プリセットは別の雲システムではなく、**既存の天気軸への窓**。
軸そのものは参考画像に対して band ごとに解いた値なので、どのプリセットも
そのフィットの上に立つ。

| | 窓 | coverageScale |
|---|---|---|
| 積乱雲（既定） | 0.42-0.70 | なし |
| 快晴 | 0.06-0.16 | cumulus 0.30 / altocumulus 2.4 / cirrus 1.8 |

- 積乱雲の窓を 0.42-0.70 に狭めたのは、生の振動（0.19-0.94）だと塔が1本も
  出ない時間帯があるから。「積乱雲」ボタンが積乱雲を出さないのは壊れている。
- 快晴の 3つの scale は天気軸だけでは表せない「下は空っぽ、上は賑やか」を
  作るため。特に cumulus は**晴れた日ほど増える**設計（それが晴天の雲）
  なので、放っておくと下空がポップコーンで埋まる。
- URL でも指定できる（`?preset=clear`）。キャプチャで指定するため。
- ボタンを押すと全スロットを `generation = NaN` にして即座に作り直す。
  自然に入れ替わるのを待つと 1倍で最大 102分かかる。

**検証**: playwright でボタンを押して canvas が変わることを確認済み
（PNG 1.66MB → 1.54MB、`aria-pressed` も正しく入れ替わる）。

## 2.8 スライダ4本・時刻・雨・毎回ランダム（3回目の続き）

プリセットのボタン2つは**雲量スライダ**に置き換えた。名前のついた空2つは、
もともと連続な軸の上の2点でしかない。

### コンソール

| スライダ | 範囲 | 既定 |
|---|---|---|
| CLOUD | 0-1（天気軸そのもの） | 0.62 |
| RAIN | 0-1 | 0 |
| TIME | 12:00-19:00 | 12:00 |
| SPEED | 1-30倍 | 10倍 |

全部 URL でも指定できる（`?cloud=0.62&rain=0.8&hour=18.6`）。**何も付けなければ
正午・雨なし・参考画像にフィットしたフレーム**が出るので、計測は今まで通り。

### 雲量軸の作り直し（`cloudField.ts` の coverageAt）

「0で雲なし、0.1で遠くと高層だけ、0.7で積乱雲、上端で雨模様」。
遠景バンクの下限を 0.62 → **0** にしたのが要（0でも雲が残っていた）。

**総量ではなく分布が動く。** 遠景バンクは 0.10 で満杯になってから雨側で少し
引き、巻雲は 0.15 で満杯になってから 0.45-0.9 で **75%まで退く**（下の雲に
隠れて見えなくなるのが本当）。だから上げると「雲が増える」より
「雲が近づいてくる」と読める。

スライダは軸そのものだが、`weatherOscillation` は ±0.06×スライダ値 の残留
ゆらぎとして残してある（一定値でも空が呼吸する。0では厳密に0）。

### 時刻（`core/daylight.ts`）

`sky.ts` は本物の散乱を解いているので、太陽を下げれば空は自力で焼ける。
問題は**物理で解いていないもの**——正午の参考画像から実測した色ランプと、
正午に描かれたイラスト。この2つには照明として当てる必要がある。

- 太陽高度は 12:00 で **55°**（参考画像のキーライトの実測値）に固定。
  最後の1時間で一気に落ちる keyframe。
- 直射光の色は Kasten-Young の大気路程＋レイリー消散から。正午の路程で
  正規化するので **12:00 では厳密に白**。
- **雲の再照明は乗算ではなく「輝度を保って照明色に置き換える」ブレンド。**
  最初に乗算でやって派手に失敗した: ランプ自体が正午の色を持っているので、
  彩度の高い夕方の光を掛けると色相が二重になる。18:36 で雲が原色のセルリアン、
  ハイライトが純黄色になった（ランプの青い影×青い空の光、白い冠×青が0の直射光）。
- キーライトは**方位を固定して仰角だけ**下げる。太陽方向へ振ると、
  main.ts が一度直したフラットライト配置をカメラ軸上で通過してしまう。
- プレート（イラスト）にも `uDayTint` を掛ける。正午の絵は自分で再照明できず、
  空だけ夕方になると合成ミスに見える。正午で白なので計測に影響しない。

**検証**: 正午フレームは日照コード導入の前後で **バイト単位で同一**。

### 雨（`effects/rainShader.ts`）

**プレート合成の直前**に置く。イラストが上から塗られるので、生き残る画素は
プレートが透明な場所＝ガラス越しの空だけ。マスクを書かずに雨が屋外に収まる。
（町の帯は濡れない。そこだけ嘘。）

暗くする方が筋よりも重要。雨は「粒が見える」からではなく「光が失われる」から
雨に見える。0では pass ごと無効。

### 毎回ちがう空

`simTime` の開始位置を `Math.random() * 200000`（約55時間ぶん、塔33回分）に
した。シーンは simTime の純関数なので、種も状態も増やさずに配置・形・位相が
全部変わる。`?t=` は今まで通り優先されるので計測は再現する。

## 2.9 スライダの実装（雲量軸・時刻・雨）と実機フィードバック

### 計測ハーネスが速くなった

`scripts/shoot.js` を追加。**1回のページ読み込みで複数枚**撮る。
`window.__sakura.set()`（main.ts）でシーンを差し替えるので、シェーダの
コンパイルとブラウザ起動がスイープ全体で1回になる。実測 4枚 11.6分（従来
`capture.js` 4回なら約16分）。残りは純粋な描画コストで、1408x768 1枚あたり
約20秒。効く摘みは `WARMUP`（既定3、全体に掛かる）と `CAPTURE_W/H`
（コストは画素数比例、見るだけなら半分にすれば約4倍速い）。

### 雲量の上端＝曇天

coverage を上げるだけでは 曇天 にならない。**幾何の問題**で、高度1.3kmの雲を
20km先に置くと仰角4°＝水平線付近にしか見えない。フレームは仰角36°まである。

→ `overcast` 層を追加（z 4.5-16.5km、雲底1.0km、20クラスタ、5レベル）。
自分の zNear を持つ独立した層にしたのは、既存層の z だけ動かすと `tierSpan`
（zNear から出る）と food違いになり、ほとんどの時間が画面外になるから。

**`TIERS` の末尾に足すこと。** スロット id は配列順に振られ、各クラスタの
乱数列の種になる。途中に挿入すると後続の層が全部シャッフルされ、正午の
キャプチャが 17% の画素で変わった。

灰色にするには `uOvercast`（雲量 0.72→1 で 0→1）。実測ランプは**日射しの
積雲から採った**もので、暗い雲底の灰色を含んでいない（暗端は隙間から見える
空のセルリアン）。厚い雲底は直射光が届かず多重散乱光だけを受ける——照明の
話なので大域ユニフォームで、0.72未満では恒等。

### 雨は「近くの筋」だけでは足りない

遠くの雨は粒が分離しないので、**白っぽい幕（雨脚）**として見える。
`curtain()` が縦に引き伸ばしたノイズで、下側（＝視線が長い水平線側）ほど強い。
筋は3枚（mist / mid / near）で、長さと細さの比が全て。最初は 40x9px の
短く太い破線になって**雪に見えた**。

### 罠: rand() を「呼ぶ場所」も契約のうち

`lerp(a, base + rand()*0.9, overcast)` は overcast が 0 でも rand() を消費する
（JS は両引数を評価する）。これで deck 層の乱数列が丸ごとずれ、変わるはずの
ない正午のフレームが 16% の画素で変わった。**種で再現性を担保している
システムでは、rand() をどこで呼ぶかは何をするかと同じくらい仕様**。

### 検証

正午（t=5580, cloud=0.62, hour=12, rain=0）は、この節の全変更の前後で
**バイト単位で同一**。

## 2.10 改善案は `improvements.md` に

ユーザーからの指摘「雨が雨に見えない」「夕焼けがイマイチ」の診断と、
総合的なクオリティアップ案を **`improvements.md`** にまとめた（未実装）。

要点だけ:

- **雨の時計が `simTime`** なので、既定10倍速では雨も10倍速で落ちている。
  止め絵のキャプチャでは絶対に見えない種類の不具合。たぶんこれが主因。
- **プレートが画面の 60.5% を覆っている**ので、雨は空（39.5%）にしか
  掛からず、町は晴れたまま。「窓の外」マスクをもう1枚作れば、雨の景色・
  ガラスの雨滴・町に落ちる雲の影・夕方の町明かり・室内の夕日が全部開く。
  **投資対効果が最大の1手。**
- **太陽は方位55°固定で、水平画角は±40.5°。つまり太陽は常に画面外。**
  夕焼けの力の大半（太陽本体・逆光・雲の縁の輝き）が構造的に使えていない。
- **雲は常に上が明るく下が暗い**（焼き込んだ `aHeight`）。夕方は逆になる
  べきで、ここを太陽高度で反転させるのが一番安い夕焼け改善。
- **正午フレームの回帰テストを機械化すべき。** このセッションだけで
  「変わらないはずの正午が変わった」を3回やっている。

## 2.11 雲の輪郭パネル（ページ下半分）

`scene/cloudMask.ts` ＋ `ui/outlinePanel.ts`。ページ下半分の空きに、上の絵と
同じ空を**白線のフラットな図**として引く。

**「重なっていたら外側だけ」は難しいどころか一番楽な場合だった。** 全部の雲を
1枚のバッファにフラット白で描いた時点で、重なった雲は一つの図形に融合している。
どのロブがどのクラスタのものか、どの輪郭が別の輪郭の内側か、を下流が知る必要は
無い——バッファがもう知らないから。その境界を辿れば和集合の外形が出る。

- マスクは 352x192（絵の 1/4）。2値化して辿るので線の太さ未満の情報は
  どのみち捨てる。CPU に読み戻すのもこの大きさなら安い。
- 輪郭は **marching squares**。曖昧な2ケース（5と10）は常に同じ側に倒す。
  正しく倒すことより一貫して倒すことの方が大事で、揺れると雲が流れるたびに
  線がちらつく。
- 辿る前に **3x3 の多数決フィルタ**。衛星ロブが1セルに落ちると4本の線で
  菱形になり、雲ではなく紙のゴミに見える。
- ビュー用カメラで描くので画角は絵と同じ。ただし**プレートは無視する**ので、
  窓枠に隠れている雲も線としては出る。これは意図的（空の読み取りであって
  写真のトレースではない）。
- 更新は 20フレームに1回。雲ジオメトリをもう一度描いて同期読み戻しをするので
  安くはない。**実機で引っかかるならこの数字が摘み**（未計測）。

## 3. 次の課題: ポップコーン

**同じ形の球が並んでいるのが見える。** 参考画像の雲は塊の中で凹凸が連続していて、
球の集合には見えない。密度を上げたぶん余計に目立つようになった。

これは `handoff.md` の旧版で未達として残っていた指標とおそらく同じ根:

| 指標 | 参考 | 前回計測時 |
|---|---|---|
| softFrac（縁の柔らかさ） | 56.6% | 69.6% |
| medEdge | 9px | 15px |
| rimFrac | 5.3% | 8.7% |
| 彩度 | 0.17 | 0.13 |

**まず測り直すこと。** 上の数字はフィールド化する前のものなので、現状の値を
取り直してから手を付ける。触る候補は `scene/clouds.ts`（パフの散らばり方・
重なり量・スケールのばらつき）と `scene/cloudNodule.ts`（1個のパフの形状）。
`cloudShader.ts` の色ランプは参考から実測したものなので**色は動かさない**。

### 済: 雲の中に空が抜ける「黒い雲」（2回目のセッション）

ユーザー報告「異常に黒い雲がある」の正体は**シェーディングが暗いのではなく、
雲の塊の内側に空が抜けている穴**だった。計測 (`scripts/darkspots.js`):

- 「空より暗い雲」は**ゼロ**。3つの天気（w=0.19 / 0.50 / 0.94）で雲の最小輝度は
  157-159、空は 96-190。つまり絶対的に黒い画素は存在しない。
- 効いたのは**周囲の雲より暗い**という相対指標。穴の画素をサンプルすると
  rgb(99,174,220)、隣のロブは純白 255 ——数ピクセルで輝度が 94 落ちる。

原因は2つあって、両方直した。

| | t=5580 | t=3030 |
|---|---|---|
| 発見時 | 0.205% | 0.215% |
| ① bulk を相似変換に | 0.185% | 0.137% |
| ② ロブ径の上限を芯/縁で変える | **0.039%** | **0.032%** |

最大の穴（連結画素数）は 309px → 28px。

① `cloudBulk` がロブの**大きさだけ**縮めて**位置**を縮めていなかったので、
   衰退中の雲が自分のロブをばらけさせて穴を開けていた（前セッションの回帰）。
   相似変換にすれば重なり量は bulk に対して不変なので、どんな bulk でも
   新しい穴は開かない。ビルド時に焼いた `burial` も有効なまま。
   雲底は凝結高度なので**雲底を基準に**縮める。

② こちらが本体で、**ポップコーンと同じ根**。`grainCap` を全ロブに一律で
   掛けていたので、上限に当たったロブが全部**同じ大きさ**になっていた。
   同じ大きさの球が詰まれば (a) ポップコーンに見え、(b) 球の隙間も全部同じで
   塞ぐ大きい球が無いので空が抜ける。上限を**芯では緩く・縁では厳しく**
   (`grainCapCore` / `grainCap`) したので、内側は大きいロブが重なって
   solid になり、輪郭は細かいスカラップのまま残る。この芯と縁の差が
   欠けていたサイズの階層そのもの。

計測（参考の塔 vs レンダの塔、`scripts/measure.js`）:

| | 参考 | レンダ |
|---|---|---|
| sd（諧調の広がり） | 18.7 | **18.7** |
| bumpR（輪郭の凹凸の半径） | 38.0px | **34.7px**（以前 41px） |
| bumpDepth | 13.9px | 10.5px |
| softFrac | 56.6% | 83.1% |
| medEdge | 9px | 22px |
| gradX | -10.0 | -5.4 |

**下3つは鵜呑みにしないこと。** クロップが揃っていない（面積 31830 対 49269）。
塔だけを同じように切り出して測り直すのが §3 の最初の仕事。

### 方針（ユーザー指示）

**ポップコーンを完全になくすのが目的ではない。** 雲が丸い塊の集まりであること
自体は正しく、参考画像もそう描かれている。問題は「同じ大きさの球が等間隔に
並んで見える」ことの方。

> サイズとか複雑性とかバランスで自動的に解消する方向

つまり狙うのは **サイズ分布と複雑さの階層**であって、「球を消す」ではない。
`softFrac` や `medEdge` を目標値に直接叩き込むような、輪郭をぼかして数字を
合わせにいく直し方は筋が悪い（削除済みのフリンジ用シェルがまさにそれで失敗して
いる。§4 参照）。ロブの大きさの分布・重なり量・階層の段数を動かして、結果として
指標が寄る形にすること。

### 測り方（天気が動くので注意）

天気が simTime で変わるため、**参考画像と比べるときは「夏空」の時刻を使う**。
`weatherAt(t) ≈ 0.5` かつ塔が見えている時刻を探してあり、

- **`?t=5580`（w=0.499、塔3本）** ← これを基準時刻にする
- 予備: `?t=16590`（w=0.504）

```sh
node scripts/capture.js /tmp/shot.png 5580
node scripts/crop.js /tmp/shot.png render_tower.png <塔の位置>
node scripts/measure.js ref_tower.png render_tower.png
node scripts/skyprofile.js /tmp/shot.png 700 1290 40 560 --bands 13

# 雲の中に空が抜けている穴（「黒い雲」）を探す。plate.webp のアルファを
# マスクに使うので、窓枠や町を誤検出しない
node scripts/darkspots.js /tmp/shot.png
```

**塔の画面位置は毎回変わる**（流れているので）。クロップの矩形は固定値では使えない。
キャプチャを見てから決めるか、最大連結成分に任せる（`measure.js` はそうしている）。

## 4. 触らなくてよいもの

- `scene/sky.ts` の大気散乱（RMSE 3.1）。
- `scene/cloudRamp.ts`（参考から実測したランプ）。
- `CLOUD_LIGHT_DIR`（gradX が参考に到達している）。
- `core/frame.ts` の切り出し規則。縦画面（448x998）では切り抜きゼロ、
  横長では従来通り上30%/下70%。
- フリンジ用の半透明シェルは廃止済み。**復活させないこと**
  （理由は `scene/cloudShader.ts` 末尾コメント）。

## 5. 落とし穴

- **キャプチャ中に `app/src` を編集しない。** vite dev server 経由なので HMR で
  結果が混ざる。バックグラウンド実行して
  `until [ -f /tmp/shot.png ]; do sleep 15; done` で待つ。
- **キャプチャは SwiftShader（CPU）で、実機の GPU とは能力が違う。**
  実際にこれで実機だけ雲が真っ黒になるバグを見逃した（§2.6）。
  **拡張機能に依存する機能を使わないこと。** 特にテクスチャのフィルタリング:
  WebGL2 のコアで線形補間できるのは RGBA16F までで、**RGBA32F は
  `OES_texture_float_linear` が要る**。SwiftShader は持っていて Android は
  持っていないことがあり、その場合サンプラは黒(0,0,0,1)を返す。
  **見た目の最終確認は実機のスクリーンショットでやってもらうこと。**
- **`capture.js` はリポジトリ直下から実行する。**
  既定は 1408x768 ＋ `fit=frame`（計測用）。実機の形を見たいときは
  `CAPTURE_W=448 CAPTURE_H=998 node scripts/capture.js /tmp/p.png 0 fit=page`。
- **キャンバスは `.stage` の大きさ。** `window.innerWidth/Height` を読む新しい
  コードを足すと縦画面で必ずずれる。
- **プレートを作り直すには参考画像が2枚とも要る。** `1786511966180.png` は
  main にしか無かったので、無ければ
  `git checkout origin/main -- 1786511966180.png`。
- **`?t=` を付けないと絵が毎回変わる。** 比較には必ず同じ t を使う。
- **クラスタは寿命が来ると作り直される。** `clouds.ts` の
  `CloudClusterHandle.dispose()` を呼ばずに捨てるとGPUリークになる。
- **`bumpR` は絶対ピクセル。** 塊の画面サイズが違うと比較できない。
- **古い参考画像（削除済み `1786418841252.png`）向けの定数がコメントに残って
  いる罠。** 統計を見たらどちらの画像のものか必ず疑う。

## 6. 未着手（優先度順）

0. **タイトルの文言**（`index.html` の `.title__text`）。枠だけ置いてある。
   下部 `.console` も、今はスライダー1つだけの空き地。
1. **ポップコーンの解消**（§3）。§2.5 でロブ形状まわりを触ったので、
   まず現状を測り直す。
2. 雨の天気の見た目。今は雲量・雲底・雲頂しか動かしていない。暗さは色をいじらず
   **密度と光学的厚みを上げて Beer-Lambert に出させる**方針（`image-sky-plan.md` §3）。
3. 塔のライフサイクルの見せ方。今は横断＝一生（102分）で、成長は高さのフェードのみ。
   半径プロファイルとパフ数にも効かせて「膨らむ」ようにする。
4. `scripts/flow.js`（移動量の検証）が未作成。`?t=0` と `?t=60` の重心シフトが
   §2 の表と一致するかを自動で見るもの。
5. 性能の実機確認（Pixel 10 Pro）。クラスタ数を 47 → 77 に増やしてあるので、
   フレームタイムを測っていない。
