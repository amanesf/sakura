# 引き継ぎ: 雲の高度分布（構図）を参考画像に合わせる

前セッション（空・雲の作り込み）からの引き継ぎ。**次にやることは1つ: 雲の
高度分布の是正。** 色・階調・縁・空はひととおり参考値に寄せ終わっていて、
残っている最大の差は「どこに雲があるか」。

## 0. 前提

- 作業ブランチ `claude/skydome-adjustment-continue-9wyu9d`（tip `4b6b04b`）。
  ここで開発し、ここに push。PR は明示的に頼まれるまで作らない。
- アプリは `app/`（vite + three.js + TypeScript）。方針は `plan.md`。
- 参考画像は **`1786443741198.png`**（リポジトリ直下）。女の子と窓枠の
  イラストなので、ガラスの内側だけを使う。
- **大原則（plan.md §2）**: 色や形を「レンダリング→目視→勘で数値を直す」で
  決めない。参考とレンダの両方に同じ統計を当てて数字で判断する。物理・気象の
  式から導ける部分は式から導く。コミットメッセージには「何をどう測って、どの
  数値がどう動いたか」を必ず書く（既存の履歴が書式の見本）。
- **「線画は足さない」** — ユーザーからの明確な禁止事項。

計測ハーネスの使い方は `scripts/README.md`。要点だけ:

```sh
node scripts/capture.js /tmp/shot.png 0          # 決定論的キャプチャ（数分かかる）
node scripts/crop.js  <in> <out> <l> <t> <w> <h>
node scripts/measure.js   ref_tower.png render_tower.png   # シルエット・陰影
node scripts/tonedist.js  ref_tower.png render_tower.png   # 階調の位置
node scripts/skyprofile.js <img> 700 1290 40 560 --bands 13 # 仰角別の空と雲
node scripts/skymodel.js --profile|--solve|--cirrus         # 大気のオフラインモデル
node scripts/hdr.js --to-hdr 162,203,227                    # sRGB ↔ 線形HDR
```

追跡用のクロップ（この2つで前セッションの表と比較できる）:

```sh
node scripts/crop.js 1786443741198.png ref_tower.png    860 90 340 250
node scripts/crop.js /tmp/shot.png     render_tower.png 760  0 480 340
```

## 1. 今回の課題: 雲の高度分布

`skyprofile.js` の雲ピクセル数を仰角帯ごとに数えた結果（窓の内側
x[700,1290) y[40,560)、1帯=40行=23600px）。**これが一次資料。**

| 画面行 | 仰角 | 参考 | 現在 | 参考% | 現在% |
|---|---|---|---|---|---|
| y40-80   | 33.0° |  1291 |  8305 |  **5%** | **35%** |
| y80-120  | 30.6° |  4558 |  7439 | 19% | 32% |
| y120-160 | 28.1° |  6730 |  9112 | 29% | 39% |
| y160-200 | 25.5° |  7297 | 12941 | 31% | 55% |
| y200-240 | 22.8° |  5399 | 11074 | 23% | 47% |
| y240-280 | 20.1° |  5898 | 11023 | 25% | 47% |
| y280-320 | 17.4° |  7523 | 12360 | 32% | 52% |
| y320-360 | 14.6° | 15298 | 14825 | 65% | 63% |
| y360-400 | 11.8° | 15721 | 14162 | 67% | 60% |
| y400-440 |  9.1° | 17807 | 10184 | **75%** | **43%** |
| y440-480 |  6.3° | 18684 |  9985 | **79%** | **42%** |
| y480-520 |  3.5° | 16607 | 20308 | 70% | **86%** |
| y520-560 |  0.8° |  9831 | 14940 | 42% | **63%** |
| 合計 | | 132644 | 156658 | 43% | 51% |

**総量はほぼ合っている（43% vs 51%）。ずれているのは配置。** 具体的には3つ:

1. **上に雲がありすぎる。** 仰角33°で参考5%に対し現在35%（7倍）。17〜28°の
   帯も軒並み1.5〜2倍。参考の空は上半分がほぼ抜けている。
2. **6〜9°に穴がある。** 参考はここが最も厚く（75〜79%）連続した中層の
   デッキになっているのに、現在は43%しかない。
3. **0〜4°に寄りすぎ。** 参考70%/42%に対し現在86%/63%。つまり裾野の塊が
   低すぎる位置に溜まっている。

要するに **「上に散らばっている雲を減らし、0〜4°に溜まっている塊を6〜15°へ
持ち上げる」**。総量を増やす話ではない。

塔の帯だけを行ごとに数えた補助データ（x[700,1300)、雲=青み≤80かつ輝度≥150）:

```
REF   y60:   0  y100:  98  y140: 163  y180: 166  y220: 129  y260: 104  y300: 121  y340: 365
NOW   y60: 227  y100: 172  y140: 233  y180: 364  y220: 291  y260: 305  y300: 321  y340: 398
```

参考の塔は y60 でゼロ（天辺が y≈77 から始まる）。現在は y60 で既に227px あり、
**天辺が画面上端で切れている。**

## 2. 幾何（ここは式から出せる）

- 垂直FOV 50°、アスペクト 1408/768 → 水平FOV ≈ 81.0°。
  横 17.4 px/度、縦 15.4 px/度。地平線は y=553（`HORIZON_SCREEN_FRACTION` 0.72）。
- 画面上端の仰角 ≈ 34.9°。行→仰角の式は `scripts/skyprofile.js` の
  `elevationDeg()` にある（`core/camera.ts` と同じピンホール幾何）。

### 主役の入道雲（`app/src/main.ts`）

現状: `TOWER_CENTER (5.5, -16)` → 水平距離 √(5.5²+16²) = **16.92 km**、
`TOWER_RADIUS 4.3`、`CLOUD_BASE_ALT 1.4`、`TOWER_TOP_ALT 10.8`。

- 角幅 = 2·atan(4.3/16.92) = **28.6°**。参考の塔の胴の幅は約 **15°**。→ 幅が約2倍。
- 天辺の仰角 = atan(10.8/16.92) = **32.6°**。参考の天辺は y≈77 ＝ 約 **33°**。
  → **高さ（TOWER_TOP_ALT）はほぼ正しい。** 直すのは幅と、下部の露出。
- 底の仰角 = atan(1.4/16.92) = 4.7°。一方 **参考では塔の下半分は中層デッキに
  隠れていて、見えているのは約14.6°から上だけ。**

つまり塔が上端で切れているのは「背が高すぎる」のではなく **胴が太すぎて肩が
上端に届いている**のと、**下半分を隠すデッキが無い**ため。

### 中層デッキ（`app/src/main.ts` の `BANK_TIERS`）

現状:

| tier | count | z | baseAlt | top | radius |
|---|---|---|---|---|---|
| 0 | 16 | 26〜42km | 1.5 | 2.4〜4.4 | 2.6〜5.0 |
| 1 | 20 | 48〜82km | 1.8 | 4.0〜8.0 | 4.0〜9.0 |

天端の仰角は tier0 が atan(4.4/34) ≈ **7.4°**、tier1 が atan(8/65) ≈ **7.0°**。
参考のデッキは **14.6°** まで届いている。だから 6〜9° に穴が空き、塊が 0〜4° に
溜まる。

**参考の 6〜15° の厚い層は「低い積雲のバンク」ではなく中層の雲層**（気象で言えば
高積雲）。距離34kmで14.6°に届かせるには高度 8.9km が必要で、これは「低い
バンク」としては無理がある。素直なのは **もっと近い（15〜25km）中層のティアを
足すか、tier0 をそこへ引き上げる**こと。距離20kmなら 14.6° は高度5.2km。

## 3. 進め方の提案

1. まず `BANK_TIERS` を触って 6〜15° を埋める（塔には触らない）。ここが穴なので
   効果が一番大きく、かつ塔を隠すのでステップ2の効きが読みやすくなる。
2. 次に `TOWER_RADIUS` を落として角幅 28.6° → 15° に寄せる。距離を変えず幅だけ
   なら 4.3 → 約 2.2km。ただし単独でやると縦長の針になるので **必ず1のあとに**。
   `TOWER_TOP_ALT` は動かさなくてよい（天辺の仰角は既に合っている）。
3. 上の帯（17〜33°）の余りは、1と2で塔が締まれば連動して下がるはず。それでも
   残るなら `SMALL_CUMULUS` の配置を見る。
4. 変更のたびにキャプチャ＆計測し、§1の表と §4 の表の両方で前後比較する。

## 4. 現在の到達点（デグレ検出用。参考 → 現在）

塔クロップ `measure.js` / `tonedist.js`:

| 指標 | 参考 | 現在 | 状態 |
|---|---|---|---|
| lateral | 11.3 | 12.7 | ほぼ到達 |
| gradX | -10.0 | -9.9 | 到達 |
| gradY | -6.1 | -7.7 | やや過剰 |
| sd | 18.7 | 22.0 | 過剰 |
| softFrac | 56.6% | 59.2% | ほぼ到達 |
| medEdge | 9px | 12px | やや過剰 |
| rimFrac | 5.3% | 8.5% | **過剰（未達）** |
| bumpDepth | 13.9 | 14.3 | 到達 |
| bumpR | 38.0px | 49.7px | §5の注意を読むこと |
| 中央値 | 230 | 224 | ほぼ到達 |
| <205 | 20.4% | 28.5% | やや過剰 |
| ≥248 | 25.4% | 25.8% | 到達 |
| 彩度 | 0.17 | 0.17 | 到達 |

空（`skyprofile.js` の sky 列、オフラインモデルのRMSEは 22.4 → 3.1）:
全13帯で参考との輝度差は最大6、彩度差は0.05以内。**ここは触らないこと。**

裾野バンクの雲輝度（仰角0.8°）: 参考 193.1、開始時 248.6、現在 211.2。
まだ +18 明るい。§1の分布を直すと連動して動くはずなので、先に分布を見る。

## 5. 落とし穴（前セッションで実際に踏んだもの）

- **キャプチャ中に `app/src` を編集しない。** vite dev server 経由なので HMR で
  結果が混ざる。バックグラウンド実行して待つ。
- **`pgrep -f "capture.js /tmp"` で待つと、Monitor 自身の until ループの
  コマンド文字列にマッチして「まだ動いている」と誤判定する。** 出力ファイルの
  存在と `ps -eo pid,etime,cmd` で確認すること。
- **`measure.js` / `tonedist.js` は「最大の連結した雲の塊」に対して計算する。**
  分布を変えると塔と中層デッキが連結して、統計が一気に無意味になる。出力の
  `area` を毎回見ること（急に増えたら連結を疑う）。今回の課題はまさに連結を
  起こしやすいので、必要ならクロップの上端・下端を切り直す。
- **`bumpR` は絶対ピクセル。** 塊の画面サイズが違うと比較できない。質量で
  正規化すると 参考 38.0/√31830 = **0.213**、現在 49.7/√83689 = **0.172** で、
  相対的にはすでに参考より細かい。**塔を小さくすれば bumpR は自動的に下がる
  ので、「bumpR が改善した」と誤読しないこと。** ロープをさらに小さくする方向へ
  走る必要はない。
- **古い参考画像の数値がコメントに残っている罠。** 削除済みの
  `1786418841252.png` に合わせた定数が複数あり（例:「雲内部の48%が輝度205未満」
  → 現行画像では20.4%）、それが「塔が暗すぎる」「裾野が白すぎる」の直接原因
  だった。コメントの統計を見たら、どちらの画像のものか必ず疑う。
- **ヘイズ・ハイライト・ブルームは連結している。** `uHazeStart` を 12→16km に
  したとき、主役の塔のヘイズが 0.15→0.05 に下がり、`(1-haze)` でゲートされた
  ハイライトブーストとブルーム閾値の両方が反応して、**塔が空へグレアの輪を
  吹いた**（softFrac 54%→84%、medEdge 9→16px、lateral 12.0→2.7）。距離項を
  いじるときは塔側のヘイズ値も必ず計算してから。
- **縁の柔らかさ（medEdge/softFrac）は今やブルームが供給している。**
  `core/postFx.ts` のブルームを触ったら必ず `measure.js` で確認する。
- **フリンジ用の半透明シェルは廃止済み。復活させないこと。** 理由は
  `scene/cloudShader.ts` の末尾コメントに全部書いてある（Fresnel は球では殻の
  外縁で最大になるので、構造上「はっきりした輪郭の環」にしかならない）。

## 6. 触らなくてよいもの

- `scene/sky.ts` の大気散乱・ヘイズ・彩度（参考にフィット済み、RMSE 3.1）。
- `scene/cloudRamp.ts`（参考から実測したランプ。レンダラの露出やトーンマップを
  変えたら再生成が必要、と冒頭コメントにある）。
- `CLOUD_LIGHT_DIR`（`main.ts`、現在 `(-0.78, 0.45, -0.44)`）。gradX が -9.9 で
  参考 -10.0 に到達しているので、動かす理由がない。
