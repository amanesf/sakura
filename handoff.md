# 引き継ぎ: 雲の高度分布は是正済み、次は塔の縁と彩度

前回の課題だった「雲の高度分布」は完了。**次にやることは2つ: (a) 17-20°帯の
超過を詰める、(b) 塔の縁（softFrac/medEdge/rimFrac）と彩度の未達を潰す。**

## 0. 前提

- 作業ブランチ `claude/handoff-review-tr7qig`。ここで開発し、ここに push。
  main へは反映済み。PR は明示的に頼まれるまで作らない。
- アプリは `app/`（vite + three.js + TypeScript）。方針は `plan.md`。
- 参考画像は **`1786443741198.png`**（リポジトリ直下）。ガラスの内側だけを使う。
- **大原則（plan.md §2）**: 色や形を「レンダリング→目視→勘で数値を直す」で
  決めない。参考とレンダの両方に同じ統計を当てて数字で判断する。コミット
  メッセージには「何をどう測って、どの数値がどう動いたか」を必ず書く。
- **「線画は足さない」** — ユーザーからの明確な禁止事項。
- 計測ハーネスの使い方は `scripts/README.md`。**`scripts/capture.js` は
  playwright を require するが root の package.json には入っていない。**
  セッション開始時に一度
  `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm install --no-save playwright` が必要
  （ブラウザは `/opt/pw-browsers` に既存のものを使う）。1キャプチャ約4分。

## 1. 前回の成果: 高度分布（`skyprofile.js` の雲ピクセル数 / 23600）

| 仰角 | 参考 | 着手時 | 現在 |
|---|---|---|---|
| 33.0° |  5.5% | 35.2% |  6.0% |
| 30.6° | 19.3% | 31.5% | 20.8% |
| 28.1° | 28.5% | 38.6% | 21.1% |
| 25.5° | 30.9% | 54.8% | 25.1% |
| 22.8° | 22.9% | 46.9% | 30.5% |
| 20.1° | 25.0% | 46.7% | **34.2%** |
| 17.4° | 31.9% | 52.4% | **43.7%** |
| 14.6° | 64.8% | 62.8% | 58.7% |
| 11.8° | 66.6% | 60.0% | 63.9% |
|  9.1° | 75.5% | 43.2% | 63.2% |
|  6.3° | 79.2% | 42.3% | 72.0% |
|  3.5° | 70.4% | 86.1% | 74.2% |
|  0.8° | 41.7% | 63.3% | 47.8% |

**帯別誤差の平均 19.8pt → 6.3pt、総雲量 51.1% → 43.2%（参考 43.2%、一致）。**

やったこと（詳細は各コミットメッセージ）:

1. `BANK_TIERS` を「仰角ターゲット」で3ティアに再設計。中層デッキ（17-27km、
   高度 2.3→7.9km ＝ 6.0°-19.8°）を新設し、6-9° の穴を埋めた。地平線帯に
   届くのは最遠ティアだけにして 0-4° の溜まりを解消。
2. 塔の角幅を実測フィット。`TOWER_RADIUS` 4.3 → 2.35、
   `towerRadiusProfile` を「下 0.6 → t=0.72 で最大 → 天辺 0.22」に。
3. `TOWER_TOP_ALT` 10.8 → 9.6（後述）。

### 次に踏むな: 「公称半径 → 角幅」を幾何で換算してはいけない

半径 1.58 と 2.23 の2キャプチャで肩の帯を実測すると、幅は 5.1° と 9.2° で、
幾何の予測（10.4° / 14.7°）の**半分程度**しかない。クラスタのパフが公称
エンベロープを埋めておらず、ブルームの寄与も塊の大きさによらずほぼ一定
ピクセルのため。**半径を動かすときは必ず2点キャプチャして傾き（肩で
8.0°/km）を取り、そこから逆算すること。**

同じ理由で **エンベロープ天端 ≠ 見えている天辺**。`TOWER_TOP_ALT` 10.8 は
atan(10.8/16.92)=32.6° で参考の天辺（y≈77≈33°）と一致していたのに、最上段の
パフ自身の半径が約1.5km 上へはみ出して画面上端（34.9°）で切れていた。9.6 に
下げて 33.0°帯が 21.7% → 6.0%（参考 5.5%）になった。

## 2. 次の課題

### (a) 17-20°帯の超過（+12pt / +9pt）

中層デッキ（tier0）の天端を上げて 20.1° の穴を埋めた副作用。デッキの天端を
下げると 14.6° 以下が痩せるトレードオフがあり、`topLo/topHi` の単純な上下では
これ以上詰まらなかった（6.0-9.0 → 5.6-7.9 で 17.4° は 47.3% → 43.7%）。
高さではなく **クラスタ数と半径の配分**（`count` / `radLo-radHi` / `xStep`）か、
ティアをもう1段足して高さのばらつきを増やす方向が未検討。

### (b) 塔の縁と彩度（`measure.js` / `tonedist.js`、参考 → 現在）

| 指標 | 参考 | 現在 | 状態 |
|---|---|---|---|
| gradX | -10.0 | -8.6 | ほぼ到達 |
| gradY | -6.1 | -8.4 | やや過剰 |
| sd | 18.7 | 21.9 | やや過剰 |
| softFrac | 56.6% | 69.6% | **過剰（未達）** |
| medEdge | 9px | 15px | **過剰（未達）** |
| rimFrac | 5.3% | 8.7% | **過剰（未達）** |
| 中央値 | 230 | 231 | 到達 |
| <205 | 20.4% | 20.2% | 到達 |
| ≥248 | 25.4% | 31.9% | やや過剰 |
| 彩度 | 0.17 | 0.13 | **未達（青みが足りない）** |

階調分布は参考に到達した。残るのは縁の柔らかさ（ブルーム由来、`core/postFx.ts`）
と彩度。**ただし `area` が 53004（参考 31830）で、塔と中層デッキが連結して
いる疑いが濃い**（handoff 前版 §5 の警告どおり）。分布を変えた後なので、まず
クロップを切り直して塔単独の `area` にしてから読むこと。現在のクロップは
`node scripts/crop.js /tmp/shot.png render_tower.png 820 60 380 300`。

## 3. 触らなくてよいもの

- `scene/sky.ts` の大気散乱・ヘイズ・彩度（参考にフィット済み、RMSE 3.1）。
  今回の分布変更後も空側の輝度差は最大6以内で、劣化していない。
- `scene/cloudRamp.ts`（参考から実測したランプ）。
- `CLOUD_LIGHT_DIR`（gradX が参考に到達しているので動かす理由がない）。
- フリンジ用の半透明シェルは廃止済み。復活させないこと（理由は
  `scene/cloudShader.ts` 末尾コメント）。

## 4. 落とし穴（前セッションから継続）

- **キャプチャ中に `app/src` を編集しない。** vite dev server 経由なので HMR で
  結果が混ざる。バックグラウンド実行して待つ。
- **`capture.js` はリポジトリ直下から実行する。** `app/` から実行すると
  `scripts/` が見つからない。
- **`measure.js` / `tonedist.js` は「最大の連結した雲の塊」に対して計算する。**
  出力の `area` を毎回見ること。
- **`bumpR` は絶対ピクセル。** 塊の画面サイズが違うと比較できない。
- **ヘイズ・ハイライト・ブルームは連結している。** 距離項をいじるときは塔側の
  ヘイズ値も必ず計算してから。
- **古い参考画像（削除済み `1786418841252.png`）向けの定数がコメントに残って
  いる罠。** 統計を見たらどちらの画像のものか必ず疑う。
