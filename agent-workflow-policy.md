# 実装フェーズの運用方針：モデル・ルーティングとサブエージェント運用

`season-transition-animation.md` の実装を進めるにあたり、メイン（設計・指示）とサブエージェント（実装）でモデルを使い分けるための方針。単純な「Haiku全振り＋難しい時だけOpus」ではなく、判断基準を先に固定しておくことでブレをなくす。

## 1. 役割とモデル

| 役割 | モデル | 担当 |
|---|---|---|
| メイン | Sonnet | 設計、タスク分解、仕様書き、サブエージェントへの指示、成果物レビュー、統合判断 |
| 実装（標準） | Haiku | 仕様が明確な機械的実装（後述） |
| 実装（難所） | Opus | 判断・数式・チューニングが必要な難所（後述、事前に基準を固定） |

## 2. Opusに回す基準（事前固定・都度判断しない）

「難しそうだから」でSonnetがその場で判断するのではなく、**該当したら自動的にOpus**というルールにする。

- シェーダー／GLSLコード（季節ティントのブレンド、headroomゲート付き発光、遷移演出の色あせ・歪み・ビネット等）
- GPUパーティクル・物理挙動の設計（花びら・落ち葉・雪・雨、タイムフィールド強度に応じた状態遷移）
- パフォーマンスチューニング（描画負荷、フレームタイム、Pixel 10 Pro実機でのプロファイリング）
- 見た目の質にかかわる微調整で反復判断が必要なもの（色温度・カメラ画角・ライティングの最終調整）
- **Haikuが同一タスクで2回失敗した場合**（3回目からやり直させず、即Opusにエスカレーション。Haikuへの再投げは無駄なコールドスタートを増やすだけなので避ける）

上記に当てはまらない実装（UIコンポーネントの組み立て、設定値の反映、既存パターンの横展開、ドキュメントに書かれた仕様通りのシーン state 実装など）はHaikuを既定とする。

## 3. エフォートは階層ごとに変える（一律「中」にしない）

| モデル | 既定エフォート | 引き上げる条件 |
|---|---|---|
| Haiku | 低 | 複数ファイルにまたがる配線作業など、多少込み入る場合のみ中 |
| Opus | 中 | シェーダー数式・パフォーマンス調査など本当に難しい場合は高 |

Haikuに高いエフォートを与えてもコストが増えるだけで実装力の上限は変わらないため、迷ったら下げる。逆にOpusを呼ぶ時点で「難所」であることは確定しているので、こちらは出し惜しみしない。

## 4. 依頼の粒度：サブシステム単位でまとめる

サブエージェントの呼び出しはコールドスタート（コンテキスト再構築）のコストがかかるため、関数単位・ファイル単位の細切れ依頼はしない。**1回の依頼＝1つのサブシステム**を基本単位にする。

例（`season-transition-animation.md` の章に対応）：
- 依頼A：木・湖・山並みの3D構図とカメラ（2章・6章）
- 依頼B：タイムフィールドの統一ゆらぎシステム（4章）＋足元の草花レイヤー（5章）
- 依頼C：タイムマシン装置UIとダイヤル操作・自動巡回（7章）
- 依頼D：光の粒子による遷移演出（3章・3.1章）
- 依頼E：桜吹雪・落葉のパーティクル散り演出（8章）

それぞれを独立した依頼としてまとめて渡し、関連するファイル・仕様・受け入れ基準をSonnetが事前にひとまとめにして渡す。依頼の中でHaikuとOpusが混在する場合（例：依頼Dは大部分Haikuだが遷移シェーダー部分だけOpus）は、Sonnetが依頼を出す時点でサブタスクごとにモデルを指定する。

## 5. Sonnet（メイン）のレビュー責任

- サブエージェントの成果物は、統合前に必ずSonnetがレビューする。Haikuの出力は特に、シェーダー・パーティクル・パフォーマンスに関わる箇所を重点的に確認し、該当すればその場でOpusに回す（2章の基準に沿って機械的に判断する）。
- レビューで見つかった問題を「Haikuに直させる」のは1回まで。直らなければOpusにエスカレーションする（3.のコールドスタート回避と同じ理由）。

## 6. まとめ

- 判断基準（Opusに回す条件）を先に固定することで、都度の判断コストとブレをなくす。
- エフォートは階層ごとに変え、Haikuは低め、Opusは難所相応に高めまで許容する。
- 依頼はサブシステム単位でまとめ、コールドスタートの無駄を減らす。
- Haikuの失敗は2回までで見切りをつけ、無駄な再投げをしない。
