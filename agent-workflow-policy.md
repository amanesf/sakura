# 実装フェーズの運用方針：モデル・ルーティングとサブエージェント運用

`season-transition-animation.md` の実装を進めるにあたり、メイン（設計・指示）とサブエージェント（実装）でモデルを使い分けるための方針。単純な「Haiku全振り＋難しい時だけOpus」ではなく、判断基準を先に固定しておくことでブレをなくす。

## 1. 役割とモデル

| 役割 | モデル | 担当 |
|---|---|---|
| メイン | Sonnet | 設計、タスク分解、仕様書き、サブエージェントへの指示、成果物レビュー、統合判断 |
| 実装（標準） | Haiku | 仕様が明確な機械的実装（後述） |
| 実装（難所） | Opus | 判断・数式・チューニングが必要な難所（後述、事前に基準を固定） |

## 2. Opusに回す基準（事前固定・都度判断しない）

「難しそうだから」でSonnetがその場で判断するのではなく、**該当したら自動的にOpus**というルールにする。

- シェーダー／GLSLコード（季節ティントのブレンド、headroomゲート付き発光、遷移演出の色あせ・歪み・ビネット等）
- GPUパーティクル・物理挙動の設計（花びら・落ち葉・雪・雨、タイムフィールド強度に応じた状態遷移）
- パフォーマンスチューニング（描画負荷、フレームタイム、Pixel 10 Pro実機でのプロファイリング）
- 見た目の質にかかわる微調整で反復判断が必要なもの（色温度・カメラ画角・ライティングの最終調整）
- **Haikuが同一タスクで2回失敗した場合**（3回目からやり直させず、即Opusにエスカレーション。Haikuへの再投げは無駄なコールドスタートを増やすだけなので避ける）

上記に当てはまらない実装（UIコンポーネントの組み立て、設定値の反映、既存パターンの横展開、ドキュメントに書かれた仕様通りのシーン state 実装など）はHaikuを既定とする。

## 3. エフォートは階層ごとに変える（一律「中」にしない）

| モデル | 既定エフォート | 引き上げる条件 |
|---|---|---|
| Haiku | 低 | 複数ファイルにまたがる配線作業など、多少込み入る場合のみ中 |
| Opus | 中 | シェーダー数式・パフォーマンス調査など本当に難しい場合は高 |

Haikuに高いエフォートを与えてもコストが増えるだけで実装力の上限は変わらないため、迷ったら下げる。逆にOpusを呼ぶ時点で「難所」であることは確定しているので、こちらは出し惜しみしない。

## 4. 依頼の粒度：サブシステム単位でまとめる、依存順に投げる

サブエージェントの呼び出しはコールドスタート（コンテキスト再構築）のコストがかかるため、関数単位・ファイル単位の細切れ依頼はしない。**1回の依頼＝1つのサブシステム**を基本単位にする。

例（`season-transition-animation.md` の章に対応）と依存順：
1. 依頼A：木・湖・山並みの3D構図とカメラ（2章・6章）― 土台。最初に単独で実施
2. 依頼A'：全6シーンの季節状態システム（冬／春満開／春散り／夏／秋紅葉／秋落葉の色・形状パラメータ、10章の色温度・コントラスト方針を含む）― Aに依存。MVP（14章）の実質的な1丁目1番地であり、他の依頼が参照する土台なのでA同様に最優先・単独で実施する
3. 依頼B：タイムフィールドの統一ゆらぎシステム（4章）＋足元の草花レイヤー（5章）― A'に依存
4. 依頼C：タイムマシン装置UIとダイヤル操作・自動巡回（7章）／依頼D：光の粒子による遷移演出（3章・3.1章）― Bに依存、CとDは互いに独立なので並列可
5. 依頼E：桜吹雪・落葉のパーティクル散り演出（8章）― B・Dに依存

依存のないもの（例：CとD）は並列に投げてよいが、依存があるものを並列にすると手戻りが起きるため順序は守る。

それぞれを独立した依頼としてまとめて渡し、関連するファイル・仕様・受け入れ基準をSonnetが事前にひとまとめにして渡す。依頼の中でHaikuとOpusが混在する場合（例：依頼Dは大部分Haikuだが遷移シェーダー部分だけOpus）は、Sonnetが依頼を出す時点でサブタスクごとにモデルを指定する。

## 5. 共有インターフェースは先に固定する

タイムフィールドの強度パラメータのように複数サブシステムが参照する共通の仕組みは、実装を各サブエージェントに委ねる前に、**Sonnetが関数シグネチャ・値域・命名だけ先に決めて渡す**。これをやらないと、コールドスタートしたサブエージェントごとに微妙に違う実装を作ってしまい、統合時に壊れる。（依頼Aの成果物に、依頼B以降が参照する共有インターフェースの雛形を含めておくとよい）

特に、**花びら・葉の「揺れ→散る」状態遷移のしきい値・API**（4章の表：フィールド強度がしきい値を超えると離脱して舞う、の実装契約）は依頼A'／Bの成果物に含めて先に固定する。依頼E（桜吹雪・落葉のパーティクル散り演出）はこれを消費する側に徹し、独自に閾値やフラグを定義しない。同じ状態を2つの依頼がそれぞれ独自実装すると統合時にズレるため、オーナーを一本化する。

## 6. レビュー方法：コードレビュー優先、キャプチャは最小限

スクリーンショットでの目視確認は**遅く、精度も低い**ため、既定の確認手段にしない。基本はコード側でのレビューで済ませ、キャプチャは本当に必要な場面に絞る。

- **既定（毎回）**：型チェック・lint・ビルド成功、purely-functionalな部分（タイムフィールドの計算式、パーティクルのライフサイクル、パラメータの値域など）へのユニットテスト、書いた仕様・受け入れ基準とのコード上の突き合わせ。これで大半のサブシステムは検証を完結させる。
- **キャプチャを使う場面は限定する**：色調・カメラ画角・ライティングなど、コードだけでは判断できない見た目の最終調整（2章のOpus基準に該当する項目）でのみ使う。しかもサブタスク1つ終えるごとではなく、**サブシステム完了時に1回まとめて確認**する程度に留める。
  - 「まとめて確認」が形骸化しないよう、チェックポイントを名指しで固定する：**依頼A'（季節状態システム）完了時**に6シーンの色・形状がそれぞれ狙い通りか、**依頼D（光の粒子による遷移演出）完了時**に3.1章の4段階（発生・浸食・暗転の縁・収束）が意図通り見えるか、の2箇所は必ずキャプチャで確認する。この2つ以外は基本コードレビューのみで進めてよい。
- サブエージェントに「返す前に毎回スクリーンショットを撮って確認しろ」とは指示しない。速度を優先し、コードレビューで拾えない見た目の懸念だけを後工程でまとめて見る。

## 7. Opusはパッチ単位で使う

難所が出ても、そのサブシステム全体をOpusにやり直させるのではなく、難しい部分（シェーダー関数1つ、パーティクルの状態遷移ロジック1つなど）だけを切り出して渡す。コストと影響範囲を局所化する。

## 8. エスカレーションを次の仕様に反映する

Opusに回した案件は「なぜHaikuでは無理だったか」を一言記録しておき、次に似た仕様を書くときのテンプレート（依頼の書き方・受け入れ基準の粒度）に反映する。同じ理由でのエスカレーションを繰り返さないようにする。

## 9. Sonnet（メイン）のレビュー責任

- サブエージェントの成果物は、統合前に必ずSonnetがレビューする（6章の通りコード中心）。Haikuの出力は特に、シェーダー・パーティクル・パフォーマンスに関わる箇所を重点的に確認し、該当すればその場でOpusに回す（2章の基準に沿って機械的に判断する）。
- レビューで見つかった問題を「Haikuに直させる」のは1回まで。直らなければOpusにエスカレーションする（4章のコールドスタート回避と同じ理由）。

## 10-A. 技術選定：さらなる効率化のためのライブラリ・手法

「①前作の流用」はまだ実際にコードへアクセスできるか未検証（このリポジトリには含まれていない）。使えるかどうかは着手前に確認する前提で、それ以外にも効率化のレバーがある。

- **pmndrsエコシステム（react-three-fiber + drei + postprocessing）**：水面反射・雲・光の粒子・ブルーム/ビネット/色収差/被写界深度が既製コンポーネントで揃っており、3.1章の遷移演出の大半をシェーダーを書かずに組める。
- **three.quarks（GPUパーティクルライブラリ）**：花びら・落ち葉・雪・雨をエディタ的にプリセット構成でき、手書きGPUパーティクルシェーダーを避けられる。
- **Theatre.js（タイムライン駆動アニメーション）**：9章のミニドラマ（起承転結）をコードではなくキーフレームデータとして作れる。反復判断が必要な演出調整をOpus行きから外せる。
- **lygia（GLSLユーティリティ集）**：ノイズ・ブレンド・トーンマッピングなど頻出のシェーダー数式を自前で導出せず既存関数を呼ぶだけにできる。Opus行きのシェーダー作業をさらに圧縮できる。

これらを組み合わせると、Opus基準（2章）に該当する作業量そのものを減らせるため、コスト効率の観点でも最も効くレバーになる。

**既製3Dアセットの活用**：クオリティ・著作権リスクがあるため不採用。木・草・地形はすべて自前でプロシージャルに生成する。

### Blender（headless／bpyスクリプト）の限定的な活用を検討

人間のDCCアーティストは介さず、**エージェントが`bpy`（Blender Python API）スクリプトを書き、`blender --background`で無人実行する**前提であれば、以下のピンポイントな箇所に限定して活用を検討する。ブランケットで全面採用はせず、まず1箇所で試して効果を確認してから広げる。

- **木のジオメトリ生成**：標準搭載のSapling Tree Genアドオンをスクリプトから叩き、自然な枝ぶりの木を手続き生成する。Three.jsコードだけで一から良い木を作るより早く高品質になりやすい。依頼A'（季節状態システム）の土台として最有力候補。
- **枝・草花の階層的な揺れ**（4章）：cloth／soft body物理シミュレーションで揺れを作り、Vertex Animation Texture（VAT）として書き出す。Three.js側は書き出されたVATをテクスチャとして読み込み、頂点シェーダーでオフセットを適用するだけの薄い再生コードになる。手書きの階層減衰アニメーションより早く自然に仕上がりやすい。
- **入道雲などの体積表現**（夏のミニドラマ）：Blenderで焼いたスプライト／フリップブックに置き換え、リアルタイムのレイマーチング雲シェーダー（Opus級の難所）を回避する。

**進め方**：まずは上記のどれか1つ（木のジオメトリ生成が最有力）をパイロットとして試し、狙った効率化・品質向上が実際に出るか確認してから他の箇所に広げる。パーティクル挙動（three.quarksで対応済み）・水面反射（dreiで対応済み）・装置UI/遷移演出（postprocessing/lygiaで対応済み）は既に十分効率化できているため、Blenderは追加しない。

**環境面の注意**：この確認作業を行ったセッションのコンテナにはBlenderが入っておらず、`apt-get install blender`は依存関係含めて解決可能なことをdry-runで確認済み（実インストールは未実施）。実装フェーズのサブエージェントは毎回コールドスタートするため、Blenderを使う依頼のたびにその場でインストールする一手間が必要になる（環境セットアップ用のsession-start hookに仕込めば自動化できる）。

## 10. まとめ

- 判断基準（Opusに回す条件）を先に固定することで、都度の判断コストとブレをなくす。
- エフォートは階層ごとに変え、Haikuは低め、Opusは難所相応に高めまで許容する。
- 依頼はサブシステム単位でまとめ、依存順に投げ、独立するものは並列化してコールドスタートの無駄を減らす。季節状態システム（依頼A'）は他すべてが依存する土台なので最優先で単独実施する。
- 共有インターフェースは最初に固定し、統合時の破綻を防ぐ。特に花びら・葉の状態遷移APIはオーナーを一本化する。
- 検証はコードレビューを既定にし、キャプチャは依頼A'完了時・依頼D完了時の2箇所に固定して行う。それ以外はコードレビューのみで進め、速度を優先する。
- Haikuの失敗は2回までで見切りをつけ、無駄な再投げをしない。エスカレーションは次の仕様書きにフィードバックする。
