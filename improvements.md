# 改善案

ユーザーからの指摘2件（雨が雨に見えない / 夕焼けがイマイチ）の診断と、
それ以外のクオリティアップ案。実装はまだしていない。

書き方の方針: **「もっと良くする」ではなく「今なにが間違っているか」**を先に
特定する。数字が出せるものは出す。フィット済み（実測ランプ・RMSE 3.1 の散乱・
`frame.ts` の切り出し）に触る案には印を付ける。

---

## 1. 雨が雨に見えない

原因は4つあると見ている。**上2つが本命**で、しかもどちらも雨の描き方の問題では
なく「雨以外が雨に付き合っていない」問題。

### 1.1 【本命】雨が10-30倍速で落ちている

`main.ts` は `postFx.setRain(rainAmount, simTime)` を渡している。`simTime` は
再生速度スライダで加速する時間なので、**既定の10倍速では雨も10倍速で落ちる**
（最大30倍）。実物の雨は毎秒数メートル落ちる＝画面を0.2秒で横断する速度なので、
その10倍は「落下」ではなく**ちらつくノイズ**として知覚される。

止め絵のキャプチャでは正しく見えるので、私の検証では捕まらなかった。
実機で動かしているユーザーにだけ見えていた種類の不具合で、
§2.6 の FloatType と同じ構図。

**直し方**: 雨に専用の時計を持たせる。雨は空のような「simTime の純関数」で
ある必要がない（後処理であって、シーンの状態ではない）ので、実時間 `dt` で
進めればよい。キャプチャの再現性は `?t=` から雨時計も決めることで保つ。

雲の速度と雨の速度が食い違うことになるが、それは**正しい**。倍速で見ている
のは「天気の移り変わり」であって、雨粒ではない。

### 1.2 【本命】画面の 60.5% に雨が降っていない

雨パスはプレート合成の**直前**にあり、プレートが上から塗られる。plate.webp の
アルファを数えると **空 39.5% / 描かれた絵 60.5%**。つまり雨は空にしか掛からず、
**町・丘・海・窓枠は晴れたまま**。空が土砂降りなのに町が緑鮮やかに晴れている
——これは「雨に見えない」の最大の理由になりうる。目は地面の側を信じる。

雨は「降っているものが見える」より先に「**遠くが消える**」現象でもある。
本降りの町は彩度を失い、遠い丘は霞に溶け、海と空の境目が無くなる。今はどれも
起きていない。

**直し方**: 「窓の外」マスクをもう1枚作る。ユーザーが空をマゼンタで塗った
`1786511966180.png` と同じ作業を「ガラスの向こう全部」に対してやれば、
`scripts/plate.js` が2値マットを作れる。そのマスクの中だけ、雨量に応じて
彩度を落とし・コントラストを潰し・霞色へ寄せる。プレートの色を触ることに
なるが、**雨量0では恒等**なので計測には影響しない。

これは §2.7 で「絵の上には何も乗せない」と決めた方針の例外になる。理由は、
あのときの提案が**装飾**（一律のグラデ）だったのに対し、これは**天候**で、
ユーザーが明示的に求めている表現だから。

### 1.3 ガラスに何も付いていない

カメラは室内にあり、ガラス越しに外を見ている。この構図で雨を最も強く語るのは
空中の筋ではなく、**窓に当たる粒と、伝う筋**。窓の外の景色が歪む、粒が溜まって
不意に流れ落ちる、という動きは「雨」の記号として空中の筋より遥かに強い。

1.2 のマスクがあれば同じマスクで実装できる。屈折は歪み（UVオフセット）で、
粒は法線マップ的なノイズでよい。

### 1.4 雨が雲から降っていない

`curtain()` は画面座標のノイズで、**雲がどこにあるかを知らない**。実際の雨脚は
雲底から下に伸びるので、雲の下だけに現れて雲の間からは光が漏れる。今は空一面に
均等な幕が掛かる。

**直し方**: 雨のカーテンの濃さを、その画素の上方の雲量で変調する。安いのは
`cloudShadow` の light-space デプスマップの流用ではなく、雲パスのアルファを
別バッファに取ること。中コスト。

---

## 2. 夕焼けがイマイチ

### 2.1 【本命】太陽が絶対に画面に入らない

太陽の方位は 55°（`SUN_AZIMUTH_DEG`）で固定。フレームの水平画角は 81°、
つまり視軸から **±40.5°** しか映らない。**55° > 40.5° なので、太陽は
どの時刻でも画面外**。`sky.ts` には太陽本体の描画（`sunDisc`）もあるが、
一度も見えていない。

夕焼けの絵の力の大半は、太陽本体・その周りの強い輝き・逆光になった雲の縁から
来る。それが全部無い状態で「夕焼け」を色だけで作ろうとしている。

**直し方（提案）**: 方位を**時刻とともに振る**。12:00 では 55° のまま（＝実測の
キーライトと完全に一致、計測不変）、夕方に向けて 20-25° へ寄せる。太陽が
フレーム右端の外〜内に入ってきて、逆光と輝きが手に入る。

以前ユーザーは「このままでいい」と言ったが、それは**私が二択で聞いたとき**の
話で、今回「夕焼けがイマイチ」と言っている以上、根本原因として再提案する
価値がある。

### 2.2 雲が下から照らされない

`cloudShader` の `heightTerm` は焼き込んだ `aHeight`（下面 -1 / 冠 +1）で、
**常に上が明るく下が暗い**。太陽高度が下がるとこれは逆になる——夕方の雲は
**下面が燃えて上面が沈む**。今は正午の上下関係のまま色だけ夕方にしているので、
「オレンジのフィルタを掛けた昼の雲」に見える。

**直し方**: `uHeightSign`（あるいは重み）を太陽高度で連続的に反転させる。
正午で現行値、日没で反転。安い・効果が大きい・**正午で恒等**。

これは 2.1 より安全で、単独でもかなり効くはず。**最初に試すならこれ**。

### 2.3 地球影とビーナスベルトが無い

日没前後、太陽と反対側の空には**下から昇ってくる青灰色の帯**（地球の影）と、
その上の**桃色の帯**（ビーナスベルト）が出る。視軸は太陽から55°離れている
ので、これは本来まさに見えるはずの現象。

`sky.ts` は単散乱＋多重散乱近似なので、地球影（＝低層大気に直射光が
当たらない）を表現できていない。太陽高度が負のとき、低仰角ほど直射寄与を
落とす項を入れれば近似できる。**△ 散乱シェーダに触る**が、正午では恒等。

出典: [Earth's shadow — Wikipedia](https://en.wikipedia.org/wiki/Earth%27s_shadow)、
[Atmospheric Optics: Earth Shadow](https://atoptics.co.uk/blog/earth-shadow/)

### 2.4 部屋が正午のまま

プレートは正午に描かれた1枚絵なので、**光の向きが変わらない**。今は色を
掛けているだけ。夕方は光が長く水平になり、窓から差し込む形そのものが変わる。

完全な解決は無理（描かれた影は動かせない）。現実的な案は、窓の方向から
差し込む**暖色のグラデーションを室内側にだけ**足すこと。1.2 のマスクの
補集合で実装できる。

### 2.5 夕方のコントラストが足りない

実測ランプは正午の積雲のもので、明暗差は正午向けに合っている。夕方は
**光源が小さく指向性が強くなる**ので、明暗差はむしろ広がる。`uContrast` を
太陽高度で少し上げる余地がある。安いが、効果は 2.1/2.2 より小さい。

---

## 3. その他のアイデア

### 3.1 空・風景（効果が大きい順）

| 案 | 中身 | コスト | 備考 |
|---|---|---|---|
| **町に雲の影** | 雲の影が町の上を流れる。今の町は常に一様に晴れていて、空とつながって見えない最大の理由 | 中 | 1.2 のマスク＋影マップを地面に投影 |
| **夕方以降の町明かり** | 暗くなるにつれ窓明かりが点く。時刻スライダの見返りが一気に増える | 中 | マスク＋加算のドット |
| **雷** | 雲量・雨量とも高いときだけ、遠くで雲底が光る。数フレームの発光で成立 | 小 | 演出として費用対効果が非常に高い |
| **薄明の星** | 19:00 は日没直後。一番星が数個出るだけで「時間が進んだ」感が段違い | 小 | 太陽高度が負のときフェードイン |
| **鳥** | 遠くを数羽が横切る。生き物が1つ入るだけで絵が「動いている」ものになる | 小 | |
| **飛行機雲** | ごく稀に高層を1本 | 小 | 既存の cirrus 層に乗せられる |
| **突風** | 風速を低周波で揺らす。今は 7m/s 固定 | 小 | `simTime` の純関数のまま可能 |
| **ポップコーン解消** | 未着手の本丸（handoff §3） | 中 | 方針はユーザー指示済み |

### 3.2 体験

- **URL 共有**: パラメータは既に全部 URL に入る。「この空のリンクをコピー」
  ボタンはほぼタダで、作品が人に渡せるようになる。**費用対効果が最良**。
- **画像として保存**: 同上、canvas から1行。
- **UI を隠して眺めるモード**: 画面をタップでコントロールが引っ込む。
  「無限に眺められる」という当初の目的に直結。
- **時刻の自動進行**: 「一日モード」。放っておくと昼から日没まで流れる。
- **ランダムボタン**: 今は毎回ランダムだが、その場で引き直せない。

### 3.3 デザイン

- **下三分の一が空いている**。今は意図した暗がりだが、置くなら:
  現在の空を言葉で出す（「積乱雲 / 18:36 / 雨」）、共有ボタン、一日モードの
  トグル。何も置かないなら、もう少し詰めて絵を大きくする手もある。
- **CLOUD スライダに目盛り**。0/10/70/100% に意味（快晴・遠雲・積乱雲・曇天）が
  あるのに、今は数字しか出ない。小さなラベルか刻みを打てば「探索できる」
  ことが伝わる。
- **横画面**。今は縦専用。横で開くと 46vh の上限が効いて大きく切り取られる。
- **最初の1枚が出るまで**が無言。フェードインか、簡単なローディング。
- **タイトルの余白**。実機だとブラウザの URL バーの直下に詰まって見える。
  上余白をもう少し取る余地がある。

### 3.4 仕組み（作業を速く・安全にする）

- **正午フレームの回帰テスト**。このセッションだけで「変わらないはずの正午が
  変わった」を**3回**やっている（層の挿入で id がずれた / `rand()` を lerp の
  引数で呼んだ / FloatType）。`shoot.js` で撮った基準フレームをリポジトリに置き、
  バイト比較する小さなスクリプトを1本足せば、記憶ではなく機械が守れる。
  **最優先で入れる価値がある。**
- **実機での確認手段**。SwiftShader と実機 GPU の差で1件（FloatType）、
  再生速度の差で1件（雨の速さ）を見逃した。どちらもキャプチャでは原理的に
  見えない。ユーザーのスクリーンショットに頼らない方法があるとよい。
- **描画コスト**。1408x768 固定＋Kuwahara は実機で重い可能性がある。
  品質トグル（Kuwahara を切る）を持つと安全。未計測。

---

## 4. 優先順位（私の推し）

1. **雨の時計を実時間にする**（1.1）— 小さい・確実・たぶんこれが「雨に
   見えない」の主因
2. **正午の回帰テスト**（3.4）— 小さい・今後の全作業を守る
3. **雲の上下を太陽高度で反転**（2.2）— 小さい・夕焼けが一番安く良くなる
4. **「窓の外」マスクを作る**（1.2）— 中くらい。ただしこれ1枚で
   雨の景色・ガラスの雨滴・町の雲影・町明かり・室内の夕日が全部開く。
   **投資対効果が最大**
5. **太陽の方位を時刻で振る**（2.1）— 要相談。夕焼けの本丸だが、
   絵の中の光の向きとの整合を捨てる判断が要る
6. URL 共有ボタン（3.2）— ほぼタダ
7. ポップコーン（handoff §3）— 本丸だが上より緊急度は低い
