# 計画: 参考画像の空をくり抜き、その奥で今の雲エンジンを動かす

参考画像 `1786443741198.png` を**そのままアプリの絵にする**。空の部分だけをくり抜いて、
その奥で**既存の雲エンジン**（`scene/clouds.ts` + `cloudShader.ts`）が雲を沸かせては
流し、消す。女の子・室内・窓枠・丘・街は参考画像のまま動かない。
**窓の外の空だけが動いているアプリ。**

## 構成: 3層

| 層 | 中身 | 動く? |
|---|---|---|
| 前 | 参考画像のプレート（女の子・室内・窓枠・方立・丘・街・海・ガラスの映り込み） | 動かない |
| 中 | 雲（既存のエンジンがリアルタイムに描く） | 動く |
| 後 | 空（`scene/sky.ts` の大気散乱、RMSE 3.1 でフィット済み） | 天気でゆっくり変わる |

前面プレートは参考画像の**空以外の全画素**なので、絵の質はそのまま保たれる。
作るのは窓の向こう側だけ。

## 既存資産の扱い

**雲エンジンはそのまま使う。** `handoff.md` の履歴（帯別誤差 19.8pt → 6.3pt、
総雲量 43.2% で参考と一致、階調分布は到達）がそのまま「窓の外に出す空」の品質になる。
残っている未達（softFrac / medEdge / rimFrac / 彩度）は今回の作業と並行して詰める。
`cloudRamp.ts` の実測ランプ・`sky.ts` のフィット・カメラの構図はどれも参考画像に
合わせて解いてあるので、プレートと 1:1 で重なる。

## 1. プレート（空のくり抜き）— 実装済み

`scripts/plate.js` → `app/public/plate.webp`（RGBA 1408x768、127KB）。
空だったところが α=0。フレーム全体の 36.3% がくり抜かれた。

**多角形を手で描かずフラッドフィルで取る。** 窓枠・方立は空より十分暗いので
境界として機能し、塗りつぶしなら枠の描かれたままの輪郭が出る（直線で近似しない）。
シードは開口部ごとに1点ずつ。空の判定は3節に分かれる:

- 最も暗い素抜けの空（27,118,188、L=104）と、そのすぐ上の窓枠（66,100,125、L=95）は
  **輝度で9しか違わず、閾値では分けられない**。分けているのは彩度で、空は B-G=70、
  枠は 25。よって素抜けの空は B-G>40 で取る。
- 地平線の暗い雲の帯（102,165,208、B-G=43→25）はそれより灰色なので L>150 かつ
  B-G>20。枠は全部 L<95 なので混ざらない。
- 白い雲（254,255,255）は B-G が無いので輝度 L>200 で取る。

下端は**地形のシルエットで切る**。丘（79,127,141）は空より暗く（L=118 対 210）、
かつ暗い雲より灰色（B-G=14 対 45）なので、この2条件の組で追跡できる。
海の水平線は全幅で y=593（拡大して確認済み）。

**未実装（次の段）**: ガラスの映り込み（人物像・左上の枠の反射）の部分アルファ、
女の子の左右のパネル。今は大パネルと方立の右のみくり抜いてある。

- **くり抜く範囲**: ガラスの内側で、丘・街・海より上。大パネル
  （x∈[665,1285], y∈[8,560]）、中央パネル、方立の右（x>1350）、左奥の窓
  （x 30-260 付近）。
- **境界の性質で扱いを分ける**:
  - 窓枠・方立・丘の稜線 → 硬いエッジ。1px のフェザーだけ。
  - ガラスの映り込み（人物像 x 660-760 y 380-520、左上を斜めに横切る枠の反射）・
    汚れ・雨だれの筋・光のフレア → **部分アルファ**。ここは「空 × ガラス」の合成
    結果なので、α=0 だと映り込みが消え、α=1 だと空が動かない。観測色から空の色を
    引いて解く（`C_obs = α·C_glass + (1-α)·C_sky`）。
  - 参考画像の雲 → **プレートに残さず全部くり抜く。** その役は雲エンジンがやる。
- 検証: プレートを「参考画像の空＋雲」の上に戻して合成し、参考画像との
  RMSE < 2 / 最大差 < 12。

## 2. 雲を「沸いては消える」ようにする（`main.ts`）

今の `main.ts` は**固定された 47 個のクラスタを置いて微妙に揺らしているだけ**。
これを寿命のあるフィールドにする。

- **スポーン / デスポーン**: 風下へ抜けたクラスタを、風上の外側で**新しい種で
  作り直す**（今は `WRAP_DISTANCE` の剰余で同じ雲が戻ってくる）。種が変われば形も
  大きさも高さも変わるので、パターンは無限に続く。
- **寿命**: `towerGrowth()` はある。周期 `TOWER_CYCLE_SECONDS = 260` が短すぎるので
  **1800秒**へ（積乱雲の一生は 30-60 分）。成長は高さだけでなく半径プロファイルと
  パフ数にも効かせる（湧き上がりが「伸びる」だけでなく「膨らむ」）。
- **移流**: 世界座標で風速を与えれば画面上の速度は幾何から出る
  （f = 823.5 px/rad）。風 7 m/s のとき:

  | 距離 | 画面速度 | フレーム横断 |
  |---|---|---|
  | 16.9 km（塔） | 0.36 px/s | 69分 |
  | 38 km | 0.16 px/s | 155分 |
  | 70 km | 0.09 px/s | 4.8時間 |

  塔の `windScale 0.05`（構図アンカーのための抑制）は**外す**。塔も流す。
  一生 30-60 分と横断 69 分が同じオーダーなので、「流れながら生まれて消える」に
  自然になる。
- **揺らぎ**: 対流の時定数 L/w = 1km / 7m/s = 140秒。既存のノイズ移流の時定数を
  これに合わせる。

## 3. 天気（快晴 ⇄ 雨が降りそう）

`simTime` の低周波ノイズ（周期は数十分〜数時間）で決まる天気パラメータが、
スポーン器と雲の厚みを動かす:

| 天気 | クラスタ密度 | 雲底 | 雲頂 | 光学的厚み |
|---|---|---|---|---|
| 快晴 | 低 | 高い | 低い（積雲だけ） | 薄い |
| 夏空（参考画像の状態） | 中 | 1.4km | 塔 9.6km | 現行 |
| 雨が降りそう | 高 | 低い（0.6km） | 平たく広い | 厚い |

**暗さは色をいじって作らない。** 雲底が暗くなるのは光学的厚みが増えて太陽光が
底まで届かないからで、`cloudShader.ts` は既に Beer-Lambert とマルチスキャッタリング
近似を持っている。**密度と厚みを上げれば暗さは式から出る**（`cloudRamp.ts` の実測
ランプはそのまま）。空側は `sky.ts` の濁度（ミー散乱）を上げる。

配置密度は感覚で置かない。夏空の状態で `skyprofile.js` の帯別被覆率が参考画像に
一致する（33.0° 5.5% / 25.5° 30.9% / 6.3° 79.2% / 0.8° 41.7%）ことを基準点にして、
天気パラメータはそこからのスケールとして定義する。

## 4. 倍速スライダー（1-30倍）

- シーンの状態を **`simTime` の純関数**にする（毎フレーム加算しない）。
  `simTime` は倍速を積分した1つのスカラーで、`?t=<秒>` はこれを直接指定する。
- これを守らないと、倍速を触った瞬間に `capture.js` の再現性が壊れる
  （計測ハーネス全体がこれに依存している）。
- スポーンも「時刻の関数」で決める（クラスタ i の n 番目の世代 = 決定論的な種）。

## 5. 表示 — 対象端末は Pixel 10 Pro の横画面

- 横画面のビューポートは **約 998x448 CSS px（アスペクト 2.23）**、プレートは
  1408x768（1.833）。**引き伸ばさない。** 画角の合ったサブ矩形を切り出して、
  3Dカメラ（`setViewOffset`）とプレート（UV）の**両方に同じ矩形**を渡す
  （`core/frame.ts`）。片方だけ伸ばすと窓枠と空がずれる。
- 縦の切り落としは**上 30% / 下 70%**。塔の冠が参考画像の y≈77 にあり、床は
  失っても構わないため。2.23 なら上を 41px 落として冠に 36px の余裕が残る。
- 雲は窓の開口部でしか見えない。PostFX をフレーム全体に掛けてから最後にプレートを
  重ねるので、「窓の中だけに掛ける」はプレート自身がマスクになって自動的に成立する。

## 6. PostFX

ブルームと `gradeShader` は雲のためのものなので**窓の中だけに掛ける**。
Kuwahara・macroContrast も同じ（プレートに掛けると参考画像の絵が壊れる）。
順序としては「雲を描く → 窓の中だけに PostFX → プレートを合成」。

## 7. 順番

1. プレート（空のくり抜き）。単色の空を重ねた時点でアプリの見た目はほぼ完成する
2. 合成パイプライン（雲 → PostFX → プレート、レターボックス）
3. スポーン / デスポーンと寿命（沸いては消える）
4. 移流を実速度に（塔のアンカーを外す）
5. 天気パラメータ
6. 倍速スライダー
7. 検証（`scripts/flow.js` を追加: 移動量が §2 の表と一致、`skyprofile.js` の帯別
   被覆率が夏空で参考 ±5pt、`tonedist.js` の色が参考 ±2）

## 8. やらないこと

夕焼け（時間帯の弧）。女の子のアニメーション（瞬き・髪の揺れ）。線画は足さない。
