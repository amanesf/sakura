# 画像ベースの空 + リアルな空シミュレーション 計画

参考画像 `1786443741198.png` の空を切り抜いて素材にし、**実際の風で流れる空**として
シミュレーションする。`plan.md` §3.3-3.4（ボリューメトリック雲のレイマーチ / 手続き
生成）を置き換える計画。

**確定した方針（ユーザー指示 2026-08-12）**

- 塔は構図に固定しない。**リアルな空シミュレーション**として素直に流す。
- 再生速度はスライダーで可変、**最大30倍速**。
- **夕焼け（時間帯の弧）は今回やらない。** 日中のみ。

## 0. なぜ切り替えるか

- `agent-workflow-policy.md` §1.5 は**視覚アセットの procedural 生成を全面禁止**し、
  「既存の手続き生成コードは資産ではなく負債」と明記している。現在の
  `scene/clouds.ts` + `cloudShader.ts` はまさにその負債で、参考画像に数値フィット
  させる作業を何セッションも続けている（`handoff.md` の表がその履歴）。
- フィットは進んだが、まだ帯別誤差 6.3pt、縁（softFrac 56.6%→69.6%）と彩度
  （0.17→0.13）が未達。**参考画像の画素をそのまま使えば、これらは定義上ゼロ誤差**。
- `plan.md` §2「数学的決定論」の目的は「実装者の感覚で色や形を決めない」こと。
  画素をそのまま使うのはこの原則の**最も強い形**。決定論の出番は色・形から
  **運動と配置**（風速・視差・ライフサイクル・被覆率）へ移る。

## 1. アーキテクチャ: 平面レイヤーではなく「スプライト雲のフィールド」

30倍速では塔が **2.3分でフレームを横断する**（§3.1）。横に長いタイル画像を
スクロールさせる方式だと、その周期で同じ絵が戻ってきて繰り返しが露骨に見える。
そこで:

**参考画像から切り出した個々の雲を、既存の3Dシーンにビルボードとして無限に流す。**

- 雲の**見た目**（色・形・陰影・縁）は参考画像の画素そのもの → policy §1.5 準拠、
  フィット作業ゼロ。
- 雲の**配置と運動**は手続き的 → 無限に続き、繰り返しの周期が存在しない。
- ビルボードを現行のパースペクティブカメラの世界座標に置くので、
  **視差・遠近による減速・地平線付近での縮小がすべて幾何から自動で出る**。
  レイヤーごとに px/s を手で決める必要がない。
- 既存の `main.ts` の距離構成（塔 16.9km / 中層 22-38km / 遠景 55-95km）と
  風・ラップ処理の骨格はそのまま使える。差し替わるのは「パフの集合体」→
  「テクスチャ付きビルボード」だけ。

捨てるもの: `cloudShader.ts`, `cloudNodule.ts`, `cloudShadow.ts`, `cloudRamp.ts`,
`clouds.ts` のクラスタ生成。残すもの: `camera.ts`, `sky.ts`, `solarPosition.ts`,
`renderer.ts`, `main.ts` の距離・風の骨格。

## 2. 実測済みの前提（計測済み、この計画の土台）

| 事項 | 値 | 出所 |
|---|---|---|
| 参考画像 / キャプチャ | ともに 1408x768 | `sharp` metadata, `capture.js:18` |
| カメラ vFOV / 地平線 | 50° / 画面高の 0.72 | `core/camera.ts` |
| 焦点距離 | f = 384/tan25° = **823.5 px/rad** | 上記から導出 |
| 水平画角 | 2·atan(704/823.5) = **81.0°** | 同上 |

**参考画像とレンダは同一フレーム・同一カメラとして扱われている。**
`handoff.md` の「17.4 px/deg」は f·sec²(25°) = 17.5 と一致する。よって
**切り抜きは拡大縮小せず元のピクセル座標に 1:1 で置ける**。素材の角サイズが
そのまま正しい。

### ガラス内側の使える範囲（実測）

- 大パネルの清浄な空: おおむね **x ∈ [665, 1285], y ∈ [8, 560]**（620x552 px）。
- 除外が要るもの: 左上を斜めに横切る窓枠の反射（x<780 の y<110 付近）、
  ガラスに映った人物像（x 660-760, y 380-520）、縦の方立（x≈1290-1350）、
  その右にもう一枚の空（x 1350-1408）、下端の街と海（y>560）。
- → 矩形ではなく**多角形マスク**が要る（P0）。

### 素抜けの空（行ごとの高彩度上位5%平均 = 雲に隠れていない空）

| y | 左半分 RGB | 右半分 RGB | S |
|---|---|---|---|
| 40 | 24,117,187 | 24,117,187 | 0.87 |
| 120 | 26,121,190 | 31,125,195 | 0.85 |
| 200 | 34,132,199 | 49,137,205 | 0.80 |
| 280 | 59,166,216 | 104,179,227 | 0.63 |
| 320 以下 | 99,176,220 | 94,186,232 | 0.55 → 以降は素抜けが存在しない |

y<300 では左右差が小さく（最大 ΔR≈45）、y の1次元グラデーションで書ける。
**y>320 には素抜けの空が無い** → 実測では埋まらず、外挿が要る（§4-P1）。

### マットの難しさ

彩度ヒストグラム（パネル内全画素）: S≥0.5 が 38.6%、S 0.35-0.5 が 19.1%、
S<0.35 が 42.3%。**雲の影部（S 0.30-0.44）と地平線付近のヘイズ空（S 0.15-0.40）が
重なる**ので、`bk/art-source/canopy-clusters/chromakey.js` の彩度キー方式では
分離できない。→ 空モデルからの**残差**でマットを作る（§4-P2）。

## 3. 運動モデル（決定論の適用先）

### 3.1 移流（風）

夏の日本の下層〜中層の総観風を **7 m/s** とする。ビルボードを世界座標で
この速度で流すだけで、画面上の速度は幾何から出る（`速度[px/s] = v/d × 823.5`）:

| 距離 | 1倍速 | 30倍速 | フレーム横断（1倍 / 30倍） |
|---|---|---|---|
| 16.9 km（塔） | 0.36 px/s | 10.9 px/s | 69分 / **2.3分** |
| 22 km | 0.28 px/s | 8.4 px/s | 90分 / 3.0分 |
| 38 km | 0.16 px/s | 4.9 px/s | 155分 / 5.2分 |
| 70 km | 0.09 px/s | 2.6 px/s | 4.8時間 / 9.5分 |

視差比（近い層ほど速い）はここから自動的に決まる。**速度を「それっぽく」選ばない。**

### 3.2 ライフサイクル（湧き上がりと消散）

積乱雲の一生は 30-60 分。**フレーム横断が1倍速で69分なので、雲は「流れながら
生まれて消える」**という正しい関係になる（30倍速では 1-2 分の一生が見える）。
現行の `TOWER_CYCLE_SECONDS = 260` は短すぎるので **1800秒**に改める
（根拠は気象の実数であって目視ではない）。

画像素材では新しいターレットを生やせないので、湧き上がりは:

- **塔を上下 4-6 個のサブスプライトに切る**（底部・胴・肩・冠のターレット群）。
  既存の `towerGrowth()` 曲線で上のサブスプライトほど遅れてアルファと縦スケールが
  立ち上がる → 「下から積み上がる」読みになる。
- 消散は上から順にアルファを落としつつ横に広げる（かなとこ状に散る）。

### 3.3 内部の揺らぎ（ドメインワープ）

対流の時定数は `L/w`。ターレット径 L≈1 km、上昇流 w≈7 m/s → **約140秒**。
乱流速度 ~1 m/s が 140 秒で動かす距離は約 140 m:

- 塔（16.9km）: 140m → 0.0083 rad → **6.8 px**
- 遠景（70km）: **1.6 px**

**ワープ振幅は 5-10px、周期 2-3分。** これ以上入れると絵が溶ける。
場は発散ゼロのカールノイズ（`plan.md` §3.3 の方針を運動にだけ引き継ぐ）。

### 3.4 配置（どこに何個置くか）

雲の**個数と高度分布は感覚で置かない**。`skyprofile.js` が参考画像から測った
仰角帯ごとの被覆率（33.0° で 5.5%、25.5° で 30.9%、6.3° で 79.2%、0.8° で 41.7% …
`handoff.md` §1 の「参考」列）を**そのままターゲットにして、スポーン器のパラメータ
（距離帯ごとの個数密度・サイズ分布）を解く**。前セッションが何度も追いかけていた
統計が、今回は最初から生成側の入力になる。

## 4. フェーズ

### P0. 計測と切り抜き範囲の確定（アプリ変更なし）

1. `scripts/skycut.js` を新規作成（`sharp`）。ガラス内側の多角形マスクを
   スクリプト内の定数（頂点列）として持ち、`glass-mask.png` を出す。
2. 地平線の行（海と空の境界）を実測し、`camera.ts` の 0.72（= y 553）と照合。
   ずれていたら記録だけして、**合わせに行かない**（素材の1:1配置の前提が
   崩れていないかの確認）。
3. 検証: マスク内の画素だけで `skyprofile.js` を回し、現行クロップ
   （`700 1290 40 560`）の帯別統計と一致すること。

### P1. 背景の空（backdrop）

雲を動かすと**参考画像には写っていなかった空が現れる**。ここが破綻すると全部壊れる。

1. `sky.ts` の大気散乱をそのまま背景に使う（`skymodel.js` で RMSE 3.1 まで
   フィット済み、フルフレームを物理で埋められる）。ベイク画像にしない理由は、
   y>320 に素抜けの空が無く、ベイクだと外挿が根拠を持てないから。
2. その上で、**素抜けが実測できる y<300 の帯だけ**、参考画像との残差を仰角の
   LUT（数点の補正値）として持たせる。測れるところは参考に一致、測れないところは
   物理、という切り分け。
3. 検証: 素抜け空の領域で参考との RMSE < 3、補正 LUT の値が全帯で ±6 以内
   （それ以上ずれるならフィットか仰角の対応付けを疑う）。

### P2. 雲スプライトのライブラリ化

1. アルファ = 「その画素が P1 の空モデルからどれだけ離れているか」。
   `alpha = smoothstep(t0, t1, dist(pixel, sky(x,y)))`。閾値は目分量ではなく
   **残差ヒストグラムの谷**で決める。
2. スピル抑制: 半透明画素から背景色を抜く（`C = (C_obs - (1-α)·C_bg) / α`）。
   これをやらないと縁に青が残り、動かした瞬間にフリンジとして見える。
3. 連結成分ラベリング（`measure.js` と同じ）で塊に分け、**個別のスプライトとして
   切り出す**。想定される素材:
   - 塔 1本（さらに §3.2 用に 4-6 段へ分割）
   - 中層デッキの塊 8-15 個
   - 遠景バンク・地平線帯の塊 6-10 個
   - 小さな断片・ちぎれ雲 数個
4. 塊が繋がって切れない場合（`handoff.md` §2(b) の `area` 53004 vs 31830 の警告と
   同じ問題）は、仰角帯で切る。それでも汚ければ「塔＋連結した塊」を1枚として扱う。
5. 各スプライトに**メタデータ**を持たせる（元画像での中心の仰角 → その塊が
   本来どの距離・高度にあったか、幅の km 換算、元のピクセルサイズ）。
   これが無いとビルボードを置くときに角サイズが狂う。
6. 出力: テクスチャアトラス（`app/public/sky/clouds.webp` + `clouds.json`）。
7. 検証（必須）: **切り出した全スプライトを元の位置に戻して合成し、参考画像の
   ガラス内側との RMSE < 2 / 最大差 < 12。** ここが通らなければ以降は無意味。

### P3. スプライト・フィールドのランタイム

1. `scene/cloudField.ts` を新規作成。インスタンス化したビルボード
   （`THREE.InstancedMesh` + アトラス UV）を世界座標に配置。
   カメラは既存のまま、ビルボードは常にカメラへ正対（回転はしない: 素材の
   陰影は太陽方向固定を前提にしているので、回すと光の向きが破綻する）。
2. スポーン: 風上の境界の外で生成し、風下へ抜けたら破棄（`main.ts` の
   `WRAP_DISTANCE` の考え方をそのまま距離帯ごとに）。個数密度は §3.4 で解いた値。
3. インスタンスごとの変化: 左右反転、±20% のスケール、ライフサイクル位相、
   ワープ位相。**色は変えない**（参考の画素をそのまま出す）。
4. 深度: 距離でソートして手前から奥へ正しくアルファ合成（`depthWrite: false`,
   距離ソート済みの描画順）。
5. PostFX は既定オフ（参考画像は既にグレーディング済み。Kuwahara や
   macroContrast を上から掛けると二重になる）。`?fx=1` で従来チェーンを復帰可能に。
6. 検証: `?t=0` の静止キャプチャで `skyprofile.js` の帯別被覆率が参考 ±5pt、
   `tonedist.js` の中央値・彩度が参考 ±2。

### P4. 時間コントロール（倍速スライダー）

1. `timeScale ∈ [0, 30]` のスライダー。既定は 1。
2. **決定論の維持**: シーンの状態は「シミュレーション時刻 `simTime` の純関数」
   として書く（インスタンスの位置を毎フレーム加算していかない）。`simTime` は
   `timeScale` を積分して進める1つのスカラーで、`?t=<秒>` はこれを直接指定する。
   これで倍速を触ってもキャプチャの再現性が壊れない（計測ハーネスの前提）。
3. スライダーの見た目は最小限（`plan.md` §1 の時間帯スライダーとは別物。
   時間帯＝夕焼けは今回スコープ外）。
4. 検証: `?t=0` と `?t=3600`（simTime 秒）のキャプチャが `timeScale` の値に
   よらず同一であること。

### P5. アニメーションの検証

`scripts/flow.js` を追加して数値で確認する:

- `?t=0` と `?t=60` の2枚から各距離帯の重心シフトを測り、§3.1 の表と一致すること
  （塔 21.9 px/分、遠景 5.3 px/分）。
- `?t=0..1800` を等間隔6枚キャプチャ → `skyprofile.js` の帯別被覆率の変動が ±4pt
  以内（雲が流れても空の統計が保たれること = 気象として同じ空が続いていること）。
- 同6枚で `tonedist.js` の中央値・彩度の変動が ±2 以内（ワープや半透明の重なりで
  色が溶けていないこと）。

### P6. 仕上げ

- 性能: ビルボード数百枚のインスタンス描画で、現行のボリューメトリックより桁で軽い。
  アトラスは WebP、目標 **合計 2 MB 以下**、ミップは有効（遠景で縮むため）。
- `plan.md` §3.3/3.4 を改訂、`handoff.md` を書き直し、`scripts/README.md` に
  `skycut.js` / `flow.js` を追記。

## 5. 完了条件（数値でのみ判定）

| フェーズ | 完了条件 |
|---|---|
| P0 | マスク内の帯別統計が現行クロップと一致 |
| P1 | 素抜け空 RMSE < 3、補正 LUT が全帯 ±6 以内 |
| P2 | 再合成 RMSE < 2 / 最大差 < 12 |
| P3 | 帯別被覆率が参考 ±5pt、色が参考 ±2 |
| P4 | `timeScale` を変えても `?t=` のキャプチャが同一 |
| P5 | 移動量が §3.1 の表と一致、被覆率変動 ±4pt、色変動 ±2 |
| P6 | アセット合計 2 MB 以下 |

## 6. リスク

- **素材の使い回しが見える。** 素材は1枚の参考画像から取るので、スプライトの種類は
  20-30 が上限。30倍速では次々に流れるので、同じ雲が繰り返し来ると気づかれる。
  緩和: 反転・スケール・重なり・ライフサイクル位相のばらつき。
  それでも足りなければ `GEMINI_KEY` があるとき `scripts/gemini_call.js` で
  切り抜きを参照にしたバリエーション生成（policy §1.5 の正規ルート）。
  **鍵が無くても計画は完結する。**
- **マットの縁**: アニメ塗りの雲は縁が半透明で、α を誤ると動かしたときに青い
  フリンジか白いハローが出る。P2 のスピル抑制と P5 の色変動チェックで検出する。
  `cloudShader.ts` 末尾が記録している「フリンジ用シェル」の失敗と同じ罠なので、
  **幾何的なシェルは絶対に足さない**。
- **ビルボードの平面性**: 雲が近くを通ると板であることが見える。緩和は
  「近距離帯にスプライトを置かない」（参考画像の雲も 16.9km 以遠）。
- **太陽方向の固定**: 素材の陰影は参考画像の光の向きを焼き込んでいる。スプライトを
  回転させたり左右反転を多用すると光源が矛盾する。**反転は遠景帯のみに限る**。
- **線画は足さない**（ユーザーからの明確な禁止事項）。画像ベースなので構造的に安全。

## 7. 今回やらないこと

- 夕焼け・時間帯の弧（ユーザー指示により延期）。`sky.ts` を背景に残すのは、
  ここへ戻ってこられるようにするためでもある。
- 前面（女の子・ベンチ・室内・窓枠）のレイヤー。従来どおりスコープ外。
- 既存のボリューメトリック雲の延命・微調整。**負債として描画経路から外す**
  （policy §1.5）。
