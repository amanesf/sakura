# 計画: 生成した雲を、参考画像の空の中でリアルに動かす

背景の空は参考画像 `1786443741198.png` から切り抜いたもの。**雲は生成する**
（`scripts/gemini_call.js` の image-to-image、policy §1.5 の正規ルート）。
それを無限に沸かせては消しながら流す。天気もゆっくり移り変わる（快晴 ⇄ 雨が降りそう）。

## やること

1. **雲を生成する** — 参考画像のクロップを参照にした image-to-image で、雲を種類
   ごとに大量に作る。切り抜きではないので**枚数の上限がなく、雨雲も作れる**。
2. **世界座標に置いて流す** — 既存のカメラの奥行きに置く。視差も遠近も幾何が勝手に
   やる。画面外から入って画面外へ抜ける。無限に続く。
3. **沸いては消える** — 1つ1つが成長 → 最盛 → 消散のサイクルを持つ。塔は下から
   上へ積み上がる。
4. **天気が移り変わる** — 雲量・雲底の高さ・暗さがゆっくり変化して、快晴の日も
   雨が来そうな日もある。

## 1. 雲の生成

既存のパイプラインをそのまま踏襲する（`bk/art-source/canopy-clusters/README.md`）:
**純青背景で生成 → `chromakey.js` でマット → アトラスへ**。

- 参照画像: 参考画像から該当部位をクロップして `--image` に渡す（雰囲気を文章で
  説明する依頼は policy §1.5 で禁止）。
- 作る種類:

| 種類 | 用途 | 枚数の目安 |
|---|---|---|
| 積乱雲の段（底・胴・肩・冠） | 塔。段を遅らせて立ち上げ、湧き上がりを作る | 各 4-6 |
| 積雲（小・中） | 日中の主役、快晴時 | 12-20 |
| 中層デッキの塊 | 6-15° の帯 | 10-15 |
| 裾野バンク・地平線帯 | 0-5° の帯 | 8-12 |
| ちぎれ雲 | 隙間・変化 | 6-10 |
| 雨雲（低く暗い層・降水前の平たい帯） | 雨が降りそうな天気 | 10-15 |

- 出力は `art-source/clouds/raw/`（生の応答と画像、上書きしない既存の規約）。
  マット後にアトラスへ詰めて `app/public/sky/clouds.webp` + `clouds.json`。
- **合否は数値で決める。** 生成物を参考画像の雲と同じ指標で測り、外れたものは
  採用しない（`measure.js` / `tonedist.js`、参考の値は `handoff.md` §2(b)）:
  中央値 230 / 彩度 0.17 / softFrac 56.6% / medEdge 9px / rimFrac 5.3% / sd 18.7。
  ここを通すことが、前セッションまでずっと未達だった項目をそのまま満たすことになる。
- 光の向きを揃える。参考画像は左上手前〜逆光寄り。プロンプトで固定し、
  ばらついた生成物は落とす（スプライトを回転・反転させないので、素材側で揃える）。
- **`GEMINI_KEY` が要る。現環境には無い**ので、ここが着手の前提。

## 2. 背景の空

参考画像から切り抜く。素抜けの空（雲に隠れていない空）を行ごとに測ると:

| y | RGB | 彩度 |
|---|---|---|
| 40 | 24,117,187 | 0.87 |
| 200 | 34,132,199 | 0.80 |
| 280 | 59,166,216 | 0.63 |
| 320 以下 | 素抜けの空が1画素も無い | — |

y<300 は参考の実測値をそのまま使い、**それより下は `sky.ts` の散乱**
（RMSE 3.1 でフィット済み）で埋める。雲が動けば参考に写っていない空が出てくるが、
下側は実測が存在しないので外挿の根拠がないため。天気で空の明るさを落とすのも
ここで行う。

## 3. 動き

参考画像とレンダは同じ 1408x768・同じカメラ（f = 823.5 px/rad、水平画角 81.0°）。
世界座標で風速を与えれば画面上の速度は幾何から出る。風 7 m/s のとき:

| 距離 | 画面速度 | フレーム横断 |
|---|---|---|
| 16.9 km（塔） | 0.36 px/s | 69分 |
| 38 km | 0.16 px/s | 155分 |
| 70 km | 0.09 px/s | 4.8時間 |

- 一生: 積乱雲は 30-60 分（横断69分と同じオーダー = 流れながら生まれて消える）。
- 揺らぎ: 対流の時定数 L/w = 1km / 7m/s = 140秒、振幅 5-10px。それ以上は絵が溶ける。
- 倍速スライダー 1-30倍。シーンは simTime の純関数にして `?t=` で再現可能に保つ
  （計測ハーネスがこれに依存している）。

## 4. 天気

`simTime` の低周波ノイズ（周期は数十分〜数時間）で決まる天気パラメータが、
スポーン器の入力を動かす:

| 天気 | 雲量 | 雲底 | 使う素材 |
|---|---|---|---|
| 快晴 | 低 | 高い | 小さい積雲だけ |
| 夏空（参考画像の状態） | 中 | — | 塔＋中層デッキ＋裾野 |
| 雨が降りそう | 高 | 低い | 雨雲・平たい帯、背景の空も落とす |

配置密度は感覚で置かず、夏空の状態で `skyprofile.js` の帯別被覆率が参考画像に
一致するよう解く（33.0° で 5.5%、25.5° で 30.9%、6.3° で 79.2%、0.8° で 41.7%）。
天気パラメータはそこからのスケール。

## 5. 捨てるもの

`clouds.ts` / `cloudShader.ts` / `cloudNodule.ts` / `cloudShadow.ts` / `cloudRamp.ts`
のボリューメトリック雲一式（policy §1.5「手続き生成コードは資産ではなく負債」）。
PostFX の Kuwahara・macroContrast・grade も既定オフ（生成素材は既に絵として
完成しているので二重掛けになる）。残すのは `camera.ts` / `sky.ts` / `renderer.ts` /
`solarPosition.ts`。

## 6. 順番

1. 雲の生成（`GEMINI_KEY` 必要）。種類ごとにプロンプトと参照クロップを作り、
   `measure.js` / `tonedist.js` で採否を決めてアトラス化
2. 背景の空（参考の実測 + 下側は `sky.ts`）
3. スプライト・フィールド（世界座標・流す・沸いて消える）
4. 天気パラメータ
5. 倍速スライダー
6. 検証（`scripts/flow.js`: 移動量が §3 の表と一致、`skyprofile.js` の帯別被覆率が
   参考 ±5pt、`tonedist.js` の色が参考 ±2）

## 7. やらないこと

夕焼け（時間帯の弧）。前面レイヤー（女の子・ベンチ・窓枠）。線画は足さない。
