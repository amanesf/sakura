# 計画: 切り抜いた雲を、参考画像の空の中でリアルに動かす

参考画像 `1786443741198.png` の雲を切り抜いて素材にし、それを無限に沸かせては
消しながら流す。天気そのものもゆっくり移り変わる（快晴 ⇄ 雨が降りそう）。

## やること

1. **雲を切り抜く** — 参考画像の空から雲を1つずつ抜いてスプライトにする。
2. **世界座標に置いて流す** — 既存のカメラの奥行きに置く。視差も遠近も幾何が勝手に
   やる。画面外から入って画面外へ抜ける。無限に続く。
3. **沸いては消える** — 1つ1つが成長 → 最盛 → 消散のサイクルを持つ。塔は下から
   上へ積み上がる。
4. **天気が移り変わる** — 雲量・雲底の高さ・暗さがゆっくり変化して、快晴の日も
   雨が来そうな日もある。

## 素材（切り抜き）

- 使えるのはガラス内側の x∈[665,1285], y∈[8,560]。窓枠の反射・ガラスに映った
  人物像・方立は多角形マスクで除外。
- アルファは「空の色からどれだけ離れているか」で作る（彩度キーは不可: 雲の影部
  S 0.30-0.44 と地平線のヘイズ空 S 0.15-0.40 が重なる）。半透明画素から背景の青を
  抜かないと、動かした瞬間に青いフリンジが出る。
- 連結成分で塊に分けてスプライト化。塔は成長を段階的に出すため上下 4-6 段に分割。
  想定 20-30 枚。アトラス 1 枚（WebP、2MB 以下）。
- 切り出した全部を元位置に戻して合成し、参考画像と RMSE < 2 なら成功。

## 動き

参考画像とレンダは同じ 1408x768・同じカメラ（f = 823.5 px/rad、水平画角 81.0°）
なので、切り抜きは 1:1 で置ける。世界座標で風速を与えれば画面上の速度は幾何から出る。
風 7 m/s のとき:

| 距離 | 画面速度 | フレーム横断 |
|---|---|---|
| 16.9 km（塔） | 0.36 px/s | 69分 |
| 38 km | 0.16 px/s | 155分 |
| 70 km | 0.09 px/s | 4.8時間 |

- 一生: 積乱雲は 30-60 分（横断69分と同じオーダー = 流れながら生まれて消える）。
- 揺らぎ: 対流の時定数 L/w = 1km / 7m/s = 140秒、振幅 5-10px。それ以上は絵が溶ける。
- 倍速スライダー 1-30倍。シーンは simTime の純関数にして、`?t=` で再現可能に保つ
  （計測ハーネスがこれに依存している）。

## 天気

`simTime` の低周波ノイズで決まる天気パラメータ（周期は数十分〜数時間）が、
スポーン器の入力を動かす:

| 天気 | 雲量 | 雲底 | 素材の選び方 |
|---|---|---|---|
| 快晴 | 低 | 高い | 小さい積雲だけ |
| 夏空（参考画像の状態） | 中 | — | 塔＋中層デッキ＋裾野 |
| 雨が降りそう | 高 | 低い | 暗い塊・平たい帯を優先、空も暗く |

**参考画像には本物の雨雲が無い**（一番暗いのは地平線帯の塊と雲の影部）。
雨前の空は「雲量を上げる・雲底を下げる・暗い素材を優先・背景の空を落とす」で作る。
足りなければ `GEMINI_KEY` があるとき `gemini_call.js` で参考画像を元に雨雲素材を
生成する（policy §1.5 の正規ルート。無くても成立する）。

## 背景の空

`sky.ts`（散乱、RMSE 3.1 でフィット済み）をそのまま使う。雲が動くと参考画像に
写っていない空が出てくるが、参考画像の y>320 には素抜けの空が1画素も無いので
ベイクだと外挿の根拠がない。素抜けが測れる y<300 だけ、参考との差を仰角の LUT で
補正する。

## 捨てるもの

`clouds.ts` / `cloudShader.ts` / `cloudNodule.ts` / `cloudShadow.ts` / `cloudRamp.ts`
のボリューメトリック雲一式（policy §1.5「手続き生成コードは資産ではなく負債」）。
PostFX の Kuwahara・macroContrast・grade も既定オフ（参考画像は既にグレーディング
済み）。残すのは `camera.ts` / `sky.ts` / `renderer.ts` / `solarPosition.ts`。

## 順番

1. 多角形マスク → `scripts/skycut.js`
2. 背景の空の補正 LUT
3. スプライト切り出し + アトラス（再合成 RMSE < 2 で検証）
4. スプライト・フィールド（世界座標・流す・沸いて消える）
5. 天気パラメータ
6. 倍速スライダー
7. 検証（`scripts/flow.js`: 移動量が上の表と一致、`skyprofile.js` の帯別被覆率が
   参考 ±5pt、`tonedist.js` の色が参考 ±2）

## やらないこと

夕焼け（時間帯の弧）。前面レイヤー（女の子・ベンチ・窓枠）。線画は足さない。
