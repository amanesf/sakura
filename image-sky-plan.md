# 画像ベースの空への置き換え + 雲アニメーション 計画

参考画像 `1786443741198.png` の空部分を切り抜いて空そのものにし、その雲を動かす。
`plan.md` §3.3-3.4（ボリューメトリック雲のレイマーチ / 手続き生成）を置き換える計画。

## 0. なぜ切り替えるか

- `agent-workflow-policy.md` §1.5 は**視覚アセットの procedural 生成を全面禁止**し、
  「既存の手続き生成コードは資産ではなく負債」と明記している。現在の
  `scene/clouds.ts` + `cloudShader.ts` はまさにその負債で、参考画像に数値フィット
  させる作業を何セッションも続けている（`handoff.md` の表がその履歴）。
- フィットは進んだが、まだ帯別誤差 6.3pt、縁（softFrac 56.6%→69.6%）と彩度
  （0.17→0.13）が未達。**参考画像そのものを使えば、これらの指標は定義上ゼロ誤差**
  になる。形・色・グレーディングを「合わせる」工程自体が消える。
- `plan.md` §2「数学的決定論」の目的は「実装者の感覚で色や形を決めない」こと。
  画素をそのまま使うのはこの原則の**最も強い形**であり、違反ではない。決定論の
  出番は色・形から**運動**（風速・視差・湧き上がりの時定数）に移る。

## 1. 実測済みの前提（この計画の土台、すべて計測済み）

| 事項 | 値 | 出所 |
|---|---|---|
| 参考画像サイズ | 1408x768 | `sharp` metadata |
| キャプチャ解像度 | 1408x768 | `scripts/capture.js:18` |
| カメラ vFOV / 地平線位置 | 50° / 画面高の 0.72 | `core/camera.ts` |
| 焦点距離 | f = 384/tan25° = **823.5 px/rad**（中心 14.37 px/deg、仰角25°付近で 17.5 px/deg） | 上記から導出 |

**重要: 参考画像とレンダは同一フレーム・同一カメラモデルとして扱われている。**
`handoff.md` の「17.4 px/deg」は f·sec²(25°) = 17.5 と一致する。つまり
**切り抜きは拡大縮小せず、元のピクセル座標にそのまま置ける**（1:1）。これで
「参考の画角をどう合わせるか」という最大の不確定要素が消える。

### ガラス内側の使える範囲（実測）

- 大パネルの清浄な空: おおむね **x ∈ [665, 1285], y ∈ [8, 560]**（620x552 px）。
- 除外が要るもの: 左上を斜めに横切る窓枠の反射（x<780 の y<110 付近が暗部）、
  ガラスに映った女の子の像（x 660-760, y 380-520）、縦の方立（x≈1290-1350）、
  そこから右にもう一枚の空（x 1350-1408）、下端の街と海（y>560）。
- → 矩形ではなく**多角形マスク**が必要（P0 で作る）。

### 素抜けの空のグラデーション（行ごと、最も彩度の高い上位5%の平均 = 素抜け空）

| y | 左半分 RGB | 右半分 RGB | S |
|---|---|---|---|
| 40 | 24,117,187 | 24,117,187 | 0.87 |
| 120 | 26,121,190 | 31,125,195 | 0.85 |
| 200 | 34,132,199 | 49,137,205 | 0.80 |
| 280 | 59,166,216 | 104,179,227 | 0.63 |
| 320 以下 | 99,176,220 | 94,186,232 | 0.55 → 以降ほぼ雲に覆われ素抜けなし |

**y<300 では左右差がほぼ無く（最大 ΔR≈45, ΔG≈13）、y による1次元グラデーションで
記述できる。** y>320 は素抜けが存在しないので外挿になる（§4.2 で扱う）。

### マット（切り抜き）の難しさ

彩度ヒストグラム（パネル内全画素）: S≥0.5 が 38.6%、S 0.35-0.5 が 19.1%、
S<0.35 が 42.3%。**雲の影部（S 0.30-0.44）と地平線付近のヘイズ空（S 0.15-0.40）が
重なる**ので、単純な彩度キー（`bk/art-source/canopy-clusters/chromakey.js` 方式）
では分離できない。→ 「フィットした空グラデーションからの残差」でマットを作る（§4.3）。

## 2. 成果物

### アセット（`scripts/` でオフライン生成し `app/public/sky/` にコミット）

| ファイル | 内容 |
|---|---|
| `backdrop.png` | 素抜け空のグラデーション（フルフレーム 1408x768 以上、雲なし） |
| `cloud-hero.png` (RGBA) | 入道雲の塔と、それに繋がる塊 |
| `cloud-mid.png` (RGBA) | 中層デッキ |
| `cloud-far.png` (RGBA) | 地平線帯・裾野バンク |
| `glass-mask.png` | 参考画像のうち「空として使える画素」の多角形マスク（中間成果物） |

レイヤー分割の粒度は `main.ts` の `BANK_TIERS`（22km / 38km / 70km）と塔（16.9km）
という**既に測って決めてある距離構成**をそのまま踏襲する。視差速度がここから出る。

### ランタイム（`app/src/scene/`）

- 新規 `imageSky.ts`: フルスクリーン板 + 1本のフラグメントシェーダで
  backdrop → far → mid → hero の順にアルファ合成。各レイヤーは
  「水平スクロール量」と「カールノイズによる UV ワープ」を持つ。
- 削除（描画経路から外す。git 履歴には残る）: `clouds.ts`, `cloudShader.ts`,
  `cloudNodule.ts`, `cloudShadow.ts`, `cloudRamp.ts`, `core/buildNoise.ts` の雲用途。
- `sky.ts`（大気散乱）は**当面ランタイムから外すが削除しない**。日中は backdrop 画像
  が担当する。夕焼けへ拡張するとき（§7）に復帰候補になる。
- PostFX: 参考画像は既にグレーディング済み・絵として完成しているので、
  Kuwahara / macroContrast / grade は**既定でオフ**。ブルームだけ「動かしたときの
  縁の破綻を隠す」用途で残すか判断する（P5 で計測して決める）。

## 3. 運動モデル（ここが新しい「決定論」の対象）

色と形は参考画像から来るので、恣意的に決める余地があるのは**運動だけ**。運動は
気象の実数から導く。共通の換算は `速度[px/s] = v[km/s] / d[km] × 823.5`。

### 3.1 水平移動（風による移流）

夏の日本の下層〜中層の総観風を **7 m/s = 0.007 km/s** とする。

| レイヤー | 距離 d | 速度 | 1分あたり |
|---|---|---|---|
| hero（塔） | 16.9 km | 0.364 px/s | 21.9 px |
| mid deck | 22-38 km | 0.28-0.16 px/s | 17-10 px |
| far bank | 55-95 km | 0.11-0.06 px/s | 6.7-3.9 px |

**視差はこの表から自動的に出る**（近い層ほど速い）。速度を「それっぽく」選ばない。

塔だけは例外扱いにする余地がある: 現行 `main.ts:205` は構図アンカーのため
`windScale 0.05` を掛けている。同じ理由（塔の位置は参考画像の実測位置に合わせて
解いた）で、塔は**微小ドリフト + 揺らぎ**に留める案と、素直に流して長周期でループ
させる案がある。→ §7 の要判断項目。

### 3.2 内部の湧き上がり（ドメインワープ）

積雲の対流の時定数は `L/w`。ターレット径 L≈1 km、上昇流 w≈7 m/s → **約140秒**。
乱流速度 ~1 m/s が 140 秒で動かす距離は約 140 m。これを角度に直すと、

- 塔（16.9km）: 140m → 0.0083 rad → **6.8 px**
- far bank（70km）: **1.6 px**

つまり**ワープ振幅は 5-10 px 程度、周期 2-3 分**。これ以上入れると絵が溶ける。
ノイズ場は発散ゼロのカールノイズ（`plan.md` §3.3 の方針を運動にだけ引き継ぐ）。

### 3.3 塔の成長

画像ベースでは新しいターレットを生やせないので、`towerGrowth()` 相当は
**縦方向のわずかなスケール（±3%）と、上端のアルファのエロージョン**に留める。
実際の積乱雲の一生は30-60分なので `TOWER_CYCLE_SECONDS 260` は短すぎる →
1800秒に見直す（値の根拠は気象の実数、目視ではない）。

## 4. フェーズ

### P0. 計測と切り抜き範囲の確定（コード変更なし）

1. `scripts/skycut.js` を新規作成（`sharp`）。ガラス内側の多角形マスクを
   スクリプト内の定数（頂点列）として持ち、`glass-mask.png` を出す。
2. 地平線の行（海と空の境界）を実測し、`camera.ts` の 0.72（= y 553）と一致するか
   確認。ずれていたら backdrop の配置ではなくカメラ側の値を疑う（記録だけしておき、
   合わせに行かない）。
3. 検証: マスク内の画素だけで `skyprofile.js` を回し、現行の `700 1290 40 560`
   の帯別統計と一致することを確認（マスクが正しければ一致する）。

### P1. backdrop（素抜け空）の生成

1. マスク内の「素抜け空」画素（行ごとの高彩度上位5%）から、y の低次多項式
   （2-3次）で RGB をフィット。y<300 は実測、y>320 は外挿。
2. 外挿の妥当性チェック: `scripts/skymodel.js`（大気散乱の CPU ポート、RMSE 3.1 で
   フィット済み）の同じ仰角の値と比較する。**外挿が散乱モデルから大きく外れたら
   外挿ではなく散乱モデルの値を採る。** これで「雲が動いて現れる空」が
   デタラメにならない。
3. 水平方向の項: y<300 で左右差が最大 ΔR≈45 あるので、太陽方位由来のグローとして
   x の1次項を入れるか判断（`skymodel.js --profile` の x 依存と突き合わせる）。
4. 出力は 1408x768 より広く（**幅 2816 px = 2フレーム分**）作る。スクロール用の余白。
   グラデーションなので横に伸ばすのは無損失。
5. 検証: backdrop と参考画像の素抜け空領域の RMSE < 3（`skymodel.js` の到達値と同水準）。

### P2. 雲レイヤーのマット（切り抜き）

1. アルファ = 「その画素の色が backdrop からどれだけ離れているか」で作る。
   `alpha = smoothstep(t0, t1, dist(pixel, backdrop(x,y)))`。
   閾値 t0/t1 は目分量ではなく、**マスク内画素の残差ヒストグラムの谷**で決める
   （素抜け空のノイズ幅 = 残差の下側モードの幅から t0、雲の芯の残差から t1）。
2. スピル抑制: `chromakey.js` と同じ考え方で、半透明画素から backdrop 色を
   アンプリマルチプライで抜く（`C_cloud = (C_obs - (1-α)·C_bg) / α`）。
   これで縁に青が残らない。
3. 深度で3層に切り分ける。層の境界は**手で描いた線ではなく**、
   §1 の距離構成に対応する**仰角帯**（hero: 塔の連結成分、mid: 6.0-15.7° の帯、
   far: 1.3-4.4° の帯）を使い、`measure.js` と同じ連結成分ラベリングで塊単位に
   割り当てる。
4. 層の背後の穴（塔に隠れた mid、mid に隠れた far）は、同じ層の水平方向の
   連続性で埋める（デッキは水平の帯状なので、左右の画素の複製 + ぼかしで足りる）。
   埋めた領域は**塔がほとんど動かないので画面にはほぼ出ない**（§3.1）。
5. 検証（必須）: `backdrop + far + mid + hero` を合成した結果と参考画像の
   ガラス内側との **RMSE < 2 / 最大差 < 12**。ここが通らなければ以降は無意味。

### P3. フレーム全体を埋める

使える空は x ∈ [665,1285] の 620px 幅だけで、フレームは 1408px。**残り 44% を作る。**

- **本命（外部APIなし）**: far / mid は水平の帯状なので、切り出した帯を
  左右反転・位相ずらし・わずかな縦スケールで並べて埋める。これは
  **スクロールのためにどのみち必要なタイル化と同じ作業**。塔は一意なので複製しない
  （参考画像でも塔は右寄りにあり、左側は帯だけで正しい）。
  - 繰り返しの検出可能性を潰すため、タイルの継ぎ目は帯の切れ目（アルファ0の谷）に
    置く。継ぎ目候補はアルファのプロファイルから自動で選ぶ。
- **任意（`GEMINI_KEY` があれば）**: `scripts/gemini_call.js` で切り抜きを参照画像に
  した image-to-image で横方向に拡張し、変化のバリエーションを作る。policy §1.5 の
  正規ルート。**現環境に `GEMINI_KEY` は無い**ので、ユーザーが鍵を出せる場合の
  上積みとして扱う（本命の代替であって前提ではない）。
- 検証: 拡張後の全幅で `skyprofile.js` の帯別被覆率が、参考のガラス内側の値から
  ±5pt 以内（帯の統計を保ったまま横に伸びていること）。

### P4. ランタイムの置き換え

1. `scene/imageSky.ts`: 4枚のテクスチャを順に合成するフルスクリーンシェーダ。
   テクスチャは `LinearFilter` + `RepeatWrapping`（横のみ）。
2. `main.ts` から雲クラスタ生成・シャドウマップ・`sky.ts` を外す。
3. PostFX を既定オフにし、`?fx=1` で従来チェーンを掛けられるようにしておく。
4. 検証: `?t=0` の静止キャプチャと参考画像のガラス内側で、
   `measure.js` / `tonedist.js` / `skyprofile.js` の全指標が**参考と一致**すること
   （P2 の合成が正しければ一致する。`handoff.md` の未達項目 softFrac / medEdge /
   rimFrac / 彩度がここで全部消える）。

### P5. アニメーション

1. スクロール: §3.1 の表の速度をレイヤーごとの uniform に。テクスチャ幅で
   モジュロして無限ループ（far は 2816px タイルで約 8.9 時間周期、hero は 2.2 時間）。
2. ワープ: §3.2 の振幅・周期でカールノイズの UV ドメインワープ。
   振幅はレイヤー距離で 1/d スケール。
3. 塔の成長: §3.3。
4. 検証（新しい種類の計測。`scripts/flow.js` を追加）:
   - `?t=0` と `?t=60` の2枚をキャプチャし、各レイヤーの帯で重心シフトを測る。
     §3.1 の表の値（hero 21.9px/min など）と一致すること。
   - `?t=0..600` を等間隔6枚キャプチャし、`skyprofile.js` の帯別被覆率の
     時間変動が ±4pt 以内（動いても分布が壊れないこと）。
   - 同6枚で `tonedist.js` の中央値・彩度が ±2 以内（ワープで色が溶けていないこと）。

### P6. 仕上げ

- 性能: フルスクリーン 4 テクスチャ合成なので現行のボリューメトリックより桁で軽い。
  モバイルでのテクスチャ総量に注意（2816x768 RGBA x3 + backdrop）。
  → WebP/AVIF 化とミップ無効化でサイズを詰める。目標 **合計 2 MB 以下**。
- `plan.md` §3.3/3.4 をこの文書へのポインタ付きで改訂、`handoff.md` を書き直し。
- `scripts/README.md` に skycut / flow の使い方を追記。

## 5. 各フェーズの完了条件（数値でしか判定しない）

| フェーズ | 完了条件 |
|---|---|
| P0 | マスク内の帯別統計が現行クロップと一致 |
| P1 | 素抜け空 RMSE < 3、外挿が `skymodel.js` と整合 |
| P2 | 再合成 RMSE < 2 / 最大差 < 12 |
| P3 | 全幅の帯別被覆率が参考 ±5pt |
| P4 | `measure.js` / `tonedist.js` / `skyprofile.js` が参考と一致 |
| P5 | 移動量が §3.1 の表と一致、帯別被覆率の時間変動 ±4pt、色の変動 ±2 |
| P6 | アセット合計 2 MB 以下 |

## 6. リスク

- **マットの縁**: アニメ塗りの雲は縁が半透明（グロー）で、α の推定を誤ると
  動かしたとき縁に青いフリンジか白いハローが出る。→ P2 のスピル抑制と
  P5 の色変動チェックで検出する。`cloudShader.ts` 末尾のコメントが記録している
  「フリンジ用シェル」の失敗と同じ罠なので、**幾何的なシェルは絶対に足さない**。
- **層の分離ミス**: 塔と mid デッキが参考画像上で繋がっている（`handoff.md` §2(b) が
  `area` 53004 vs 31830 で警告していたのと同じ問題）。連結成分だけでは切れないので、
  仰角帯との併用が要る。切れ目が汚ければ「塔 + それに繋がる塊」を1レイヤーとして
  扱い、層数を2に減らす（許容する退路）。
- **動かすと繰り返しが見える**: タイルの継ぎ目と周期。§P3 の継ぎ目選択と、
  レイヤーごとに周期が無理数比になるようタイル幅を変えることで緩和。
- **線画は足さない**（ユーザーからの明確な禁止事項）。画像ベースなので構造的に安全。

## 7. 要判断（ユーザーに確認したい項目）

1. **塔を流すか、構図に固定するか。** 固定（微小な揺らぎのみ）だと参考画像の構図が
   常に保たれる。流すと2時間強でループする代わりに、塔が画面外に出る時間帯ができる。
   → 既定は**固定 + 揺らぎ**（現行 `windScale 0.05` の踏襲）を推奨。
2. **夕焼けへの拡張（`plan.md` §1 の時間帯の弧）をどうするか。** 画像ベースは
   日中1枚に固定される。選択肢は (a) 当面は日中のみ、(b) 時間帯ごとに参考画像を
   用意して切り替え、(c) 画像 + 決定論的グレーディングで擬似的に夕方へ寄せる。
   → 既定は **(a) 日中のみ**（今回のスコープ外として明記）。
3. **`GEMINI_KEY` を使えるか**（P3 の上積み・任意）。無くても計画は完結する。

## 8. 今回やらないこと

- 前面（女の子・ベンチ・室内・窓枠）のレイヤー。従来どおりスコープ外。
- 時間帯アニメーション（§7-2 で (a) を選ぶ場合）。
- 既存のボリューメトリック雲の延命・微調整。**負債として描画経路から外す**
  （policy §1.5）。
