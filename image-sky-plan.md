# 計画: 参考画像の空をくり抜き、その奥で動く空を作る

参考画像 `1786443741198.png` を**そのままアプリの絵にする**。空の部分だけをくり抜いて、
そこに生成した雲が流れる空を嵌める。女の子・室内・窓枠・丘・街は参考画像のまま動かない。
**窓の外の空だけが動いているアプリ。**

## 構成: 3層

| 層 | 中身 | 動く? |
|---|---|---|
| 前 | 参考画像のプレート（女の子・室内・窓枠・方立・丘・街・海・ガラスの映り込み） | 動かない |
| 中 | 生成した雲のスプライト（沸いて・流れて・消える） | 動く |
| 後 | 空のグラデーション（参考画像の実測値） | 天気でゆっくり変わる |

前面プレートは参考画像の**空以外の全画素**なので、絵の質はそのまま保たれる。
作るのは窓の向こう側だけ。

## 1. プレート（空のくり抜き）

`plate.png`（RGBA、1408x768）を作る。空だったところが α=0。

- **くり抜く範囲**: ガラスの内側で、丘・街・海より上。大パネル
  （x∈[665,1285], y∈[8,560]）、中央パネル、方立の右（x>1350）、左奥の窓
  （x 30-260 付近）。
- **境界の性質で扱いを分ける**:
  - 窓枠・方立・丘の稜線 → 硬いエッジ。1px のフェザーだけ。
  - ガラスの映り込み（人物像 x 660-760 y 380-520、左上を斜めに横切る枠の反射）・
    汚れ・雨だれの筋・光のフレア → **部分アルファ**。ここは「空 × ガラスの汚れ」の
    合成結果なので、α を 0 にすると映り込みが消え、1 にすると空が動かない。
    観測色から空の色を引いてガラス側の色と α を出す
    （`C_obs = α·C_glass + (1-α)·C_sky`、`C_sky` は §3 のグラデーション）。
  - 雲と空の境目 → **雲はプレートに残さない**（全部くり抜く）。奥で生成した雲が
    その役をやる。
- 検証: プレートを §3 のグラデーション＋参考画像の雲の上に戻して合成し、
  参考画像との RMSE < 2 / 最大差 < 12。

## 2. 雲（生成する）

`scripts/gemini_call.js` の image-to-image。既存パイプラインを踏襲
（`bk/art-source/canopy-clusters/README.md`）: **純青背景で生成 →
`chromakey.js` でマット → アトラス**。参照画像は参考画像から該当部位をクロップ
して渡す（雰囲気を文章で説明する依頼は policy §1.5 で禁止）。

| 種類 | 用途 | 枚数 |
|---|---|---|
| 積乱雲の段（底・胴・肩・冠） | 塔。段を遅らせて立ち上げ、湧き上がりを作る | 各 4-6 |
| 積雲（小・中） | 日中の主役、快晴時 | 12-20 |
| 中層デッキの塊 | 6-15° の帯 | 10-15 |
| 裾野バンク・地平線帯 | 0-5° の帯 | 8-12 |
| ちぎれ雲 | 隙間・変化 | 6-10 |
| 雨雲（低く暗い層・平たい帯） | 雨が降りそうな天気 | 10-15 |

- 出力 `art-source/clouds/raw/`（生の応答と画像、上書きしない既存の規約）→
  マット後にアトラス `app/public/sky/clouds.webp` + `clouds.json`。
- **採否は数値で決める。** 参考画像の雲と同じ指標で測り、外れたら落とす
  （`measure.js` / `tonedist.js`、参考値は `handoff.md` §2(b)）:
  中央値 230 / 彩度 0.17 / softFrac 56.6% / medEdge 9px / rimFrac 5.3% / sd 18.7。
- 光の向きは素材側で揃える（参考画像は左上手前〜逆光寄り）。スプライトは回転も
  反転もしない。
- **`GEMINI_KEY` が要る。現環境には無い。**

## 3. 空のグラデーション

参考画像の素抜けの空（雲に隠れていない空）を行ごとに実測した値:

| y | RGB | 彩度 |
|---|---|---|
| 40 | 24,117,187 | 0.87 |
| 200 | 34,132,199 | 0.80 |
| 280 | 59,166,216 | 0.63 |
| 320 以下 | 素抜けの空が1画素も無い | — |

y<300 は実測値、それより下は `sky.ts` の散乱（RMSE 3.1 でフィット済み）で埋める。
下側には実測が無く、外挿の根拠がないため。天気による空の明るさの変化もここで行う。

## 4. 動き

参考画像とレンダは同じ 1408x768・同じカメラ（f = 823.5 px/rad、水平画角 81.0°、
地平線は画面高の 0.72）。**プレートと空が 1:1 で重なる**のはこれが理由。
世界座標で風速を与えれば画面上の速度は幾何から出る。風 7 m/s のとき:

| 距離 | 画面速度 | フレーム横断 |
|---|---|---|
| 16.9 km（塔） | 0.36 px/s | 69分 |
| 38 km | 0.16 px/s | 155分 |
| 70 km | 0.09 px/s | 4.8時間 |

- 一生: 積乱雲は 30-60 分（横断69分と同じオーダー = 流れながら生まれて消える）。
- 揺らぎ: 対流の時定数 L/w = 1km / 7m/s = 140秒、振幅 5-10px。
- 倍速スライダー 1-30倍。シーンは simTime の純関数にして `?t=` で再現可能に保つ。
- 雲は窓の開口部でしか見えないので、描画をその範囲に絞れる（負荷が下がる）。

## 5. 天気

`simTime` の低周波ノイズ（周期は数十分〜数時間）でスポーン器の入力が動く:

| 天気 | 雲量 | 雲底 | 使う素材 |
|---|---|---|---|
| 快晴 | 低 | 高い | 小さい積雲だけ |
| 夏空（参考画像の状態） | 中 | — | 塔＋中層デッキ＋裾野 |
| 雨が降りそう | 高 | 低い | 雨雲・平たい帯、空も落とす |

配置密度は感覚で置かず、夏空の状態で `skyprofile.js` の帯別被覆率が参考画像に
一致するよう解く（33.0° で 5.5%、25.5° で 30.9%、6.3° で 79.2%、0.8° で 41.7%）。
天気パラメータはそこからのスケール。

## 6. 表示

プレートが 1408x768 の固定サイズなので、アプリはウィンドウに合わせて**アスペクト比
1.833 を保ったままフィット**させる（レターボックス）。可変アスペクトで引き伸ばすと
プレートと空がずれる。

## 7. 捨てるもの

`clouds.ts` / `cloudShader.ts` / `cloudNodule.ts` / `cloudShadow.ts` / `cloudRamp.ts`
のボリューメトリック雲一式（policy §1.5「手続き生成コードは資産ではなく負債」）。
PostFX の Kuwahara・macroContrast・grade も既定オフ（プレートは既に完成した絵で、
その上に掛けると絵が壊れる）。残すのは `camera.ts` / `sky.ts` / `renderer.ts` /
`solarPosition.ts`。

## 8. 順番

1. プレート（空のくり抜き）— 先にこれを作ると、以降は「窓の外に何を出すか」だけの
   問題になる。空を単色で塗ったプレートを重ねた時点で、アプリの見た目は完成に近い
2. 空のグラデーション（参考の実測 + 下側は `sky.ts`）
3. 雲の生成（`GEMINI_KEY` 必要）
4. スプライト・フィールド（世界座標・流す・沸いて消える）
5. 天気パラメータ
6. 倍速スライダー
7. 検証（`scripts/flow.js`: 移動量が §4 の表と一致、`skyprofile.js` の帯別被覆率が
   参考 ±5pt、`tonedist.js` の色が参考 ±2）

## 9. やらないこと

夕焼け（時間帯の弧）。女の子のアニメーション（瞬き・髪の揺れ）。線画は足さない。
